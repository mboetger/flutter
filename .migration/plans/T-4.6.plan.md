# Plan: T-4.6 — Rename AndroidShellHolder to AndroidEngine; Dissolve PlatformViewAndroid

## Proposed Changes

### 1. Rename `AndroidShellHolder` to `AndroidEngine` & Dissolve `PlatformViewAndroid`
- Rename `engine/src/flutter/shell/platform/android/android_shell_holder.h` to `android_engine.h`.
- Rename `engine/src/flutter/shell/platform/android/android_shell_holder.cc` to `android_engine.cc`.
- In `android_engine.h`:
  - Rename class `AndroidShellHolder` to `AndroidEngine`.
  - Have `AndroidEngine` inherit from `AndroidSurfaceLifecycle::Delegate` and `AndroidExternalTextureAdapter::Delegate`.
  - Add surface lifecycle methods:
    - `NotifyCreated(fml::RefPtr<AndroidNativeWindow> native_window)`
    - `NotifySurfaceWindowChanged(fml::RefPtr<AndroidNativeWindow> native_window)`
    - `NotifyChanged(const DlISize& size)`
    - `NotifyDestroyed()`
    - `SetGpuAvailability(FlutterGpuAvailability availability)`
  - Add embedder seam dispatch methods:
    - `DispatchPointerDataPacket(std::unique_ptr<PointerDataPacket> packet)`
    - `SendPointerEvents(const FlutterPointerEvent* events, size_t count)`
    - `SetViewportMetrics(int64_t view_id, const ViewportMetrics& metrics)`
    - `SendWindowMetricsEvent(const FlutterWindowMetricsEvent* event)`
    - `DispatchSemanticsAction(JNIEnv* env, jint id, jint action, jobject args, jint args_position)`
    - `DispatchSemanticsAction(uint64_t node_id, FlutterSemanticsAction action, const uint8_t* data, size_t data_length)`
    - `SetSemanticsEnabled(bool enabled)`
    - `UpdateSemanticsEnabled(bool enabled)`
    - `SetAccessibilityFeatures(int32_t flags)`
    - `UpdateAccessibilityFeatures(FlutterAccessibilityFeature features)`
    - `RegisterExternalTexture(int64_t texture_id, const fml::jni::ScopedJavaGlobalRef<jobject>& surface_texture)`
    - `RegisterSurfaceExternalTexture(int64_t texture_id, const fml::jni::ScopedJavaGlobalRef<jobject>& surface_texture)`
    - `RegisterImageTexture(int64_t texture_id, const fml::jni::ScopedJavaGlobalRef<jobject>& image_texture_entry, ImageExternalTexture::ImageLifecycle lifecycle)`
    - `RegisterImageExternalTexture(int64_t texture_id, const fml::jni::ScopedJavaGlobalRef<jobject>& image_texture_entry, ImageExternalTexture::ImageLifecycle lifecycle)`
    - `UnregisterTexture(int64_t texture_id)`
    - `UnregisterExternalTexture(int64_t texture_id)`
    - `MarkTextureFrameAvailable(int64_t texture_id)`
    - `MarkExternalTextureFrameAvailable(int64_t texture_id)`
    - `OnDisplayPlatformView(...)`
    - `DisplayPlatformView(...)`
    - `DisplayPlatformViewEmbedder(...)`
    - `OnDisplayOverlaySurface(...)`
    - `DisplayOverlaySurface(...)`
    - `OnDisplayVirtualDisplayPlatformView(...)`
    - `DisplayVirtualDisplayPlatformView(...)`
    - `DisplayVirtualDisplayPlatformViewEmbedder(...)`
    - `OnDisplayPlatformView2(...)`
    - `DisplayPlatformView2(...)`
    - `DisplayPlatformView2Embedder(...)`
    - `OnHidePlatformView2(int32_t view_id)`
    - `HidePlatformView2(int32_t view_id)`
    - `BeginFrameHC()`
    - `BeginFrameHCEmbedder()`
    - `EndFrameHC()`
    - `EndFrameHCEmbedder()`
    - `CreateOverlaySurfaceHC()`
    - `CreateOverlaySurfaceHCEmbedder(...)`
    - `DestroyOverlaySurfacesHC()`
    - `DestroyOverlaySurfacesHCEmbedder()`
    - `ScheduleFrame()`
    - `LoadDartDeferredLibrary(...)`
    - `LoadDartDeferredLibraryError(...)`
    - `UpdateAssetResolverByType(...)`
  - Add static `Register(JNIEnv* env)`.
- In `android_engine.cc`:
  - Implement the above methods using `surface_lifecycle_`, `external_texture_adapter_`, and embedder API seams directly.
  - Remove all remaining usages of `PlatformViewAndroid`.
- Delete `engine/src/flutter/shell/platform/android/platform_view_android.h` and `engine/src/flutter/shell/platform/android/platform_view_android.cc`.

### 2. JNI Implementation Updates
- In `engine/src/flutter/shell/platform/android/platform_view_android_jni_impl.cc`:
  - Include `"flutter/shell/platform/android/android_engine.h"`.
  - Update `#define ANDROID_SHELL_HOLDER` to `#define ANDROID_ENGINE (reinterpret_cast<AndroidEngine*>(shell_holder))`.
  - In `AttachJNI`, instantiate `std::make_unique<AndroidEngine>(...)`.
  - In `DestroyJNI`, delete `ANDROID_ENGINE`.
  - Implement `bool AndroidEngine::Register(JNIEnv* env)` (formerly `PlatformViewAndroid::Register`).
  - Update all JNI forwarding calls to call `ANDROID_ENGINE->...` directly.

### 3. Library Loader and Callers Update
- In `engine/src/flutter/shell/platform/android/library_loader.cc`:
  - Include `"flutter/shell/platform/android/android_engine.h"`.
  - Replace `PlatformViewAndroid::Register(env)` with `AndroidEngine::Register(env)`.
- In `engine/src/flutter/shell/platform/android/android_threading_unittests.cc`:
  - Include `"flutter/shell/platform/android/android_engine.h"`.
  - Update `AndroidShellHolder` usages to `AndroidEngine`.
- In `engine/src/flutter/shell/platform/android/android_viewport_metrics_unittests.cc`:
  - Include `"flutter/shell/platform/android/android_engine.h"`.
  - Update `AndroidShellHolder` usages to `AndroidEngine`.
- In `engine/src/flutter/shell/platform/android/platform_message_affinity_unittests.cc`:
  - Include `"flutter/shell/platform/android/android_engine.h"`.
- In `engine/src/flutter/shell/platform/android/platform_view_android_surface_lifecycle_unittests.cc`:
  - Include `"flutter/shell/platform/android/android_engine.h"`.
  - Update tests to use `AndroidEngine`.
- In `engine/src/flutter/shell/platform/android/platform_view_android_jni_impl_unittests.cc`:
  - Include `"flutter/shell/platform/android/android_engine.h"`.
  - Call `AndroidEngine::Register(&mock_env)`.

### 4. Unit Tests Consolidation
- Move `android_shell_holder_unittests.cc` to `android_engine_unittests.cc`.
- Consolidate tests from `platform_view_android_unittests.cc` into `android_engine_unittests.cc` exercising `AndroidEngine`.
- Delete `platform_view_android_unittests.cc`.

### 5. Build Configuration and Dependency Ratchet
- In `engine/src/flutter/shell/platform/android/BUILD.gn`:
  - Replace `android_shell_holder.cc`, `android_shell_holder.h`, `platform_view_android.cc`, `platform_view_android.h` with `android_engine.cc`, `android_engine.h`.
  - In unittests target, replace `android_shell_holder_unittests.cc`, `platform_view_android_unittests.cc` with `android_engine_unittests.cc`.
- Verify `python3 engine/src/flutter/tools/android_embedder_deps.py --check`.
- Update baseline if needed with `--update-baseline` ensuring counts do not increase.

## Verification Strategy
- Clang-format all touched C++ files with `engine/src/flutter/ci/format.sh --fix`.
- Verify ratchet: `python3 engine/src/flutter/tools/android_embedder_deps.py --check`.
- Run verification tool:
  `bin/cache/dart-sdk/bin/dart dev/tools/bin/migration_verify.dart --task T-4.6 --gates analysis,ratchet,native`.
