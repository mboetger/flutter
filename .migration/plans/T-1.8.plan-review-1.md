# T-1.8 Plan Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (all fields from T-0.4 parity contract mapped 1:1 into FlutterWindowMetricsEvent and ViewportMetrics)
- [x] I-5 Test targets wired (`embedder_unittests.cc` wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (new validation, round-trip parity, and backwards compatibility tests added)
- [x] I-7 Tiered diff cap respected (~180 lines)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (all changes within `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (platform-agnostic window metrics concepts applicable to Android, iOS, desktop, web)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1) and struct extension.**
   - Verified that all 17 new fields are appended to the end of `FlutterWindowMetricsEvent`.
   - Verified that no existing fields were reordered or modified.
   - Verified that `FLUTTER_ENGINE_VERSION` is untouched (§G.5, §G.9 Item 2).
   - Verified that `SAFE_ACCESS` with sensible fallbacks (`0.0` for insets/paddings, `-1.0` for unset values) is planned.
   - Result: Passed. Strict C-ABI conformance planned.

2. **Falsification Attempt 2: Verify mapping against T-0.4 parity contract.**
   - Verified that all parameters from T-0.4 are covered:
     - `physicalPadding*` -> `physical_padding_*` (top, right, bottom, left)
     - `systemGestureInset*` -> `physical_system_gesture_inset_*` (top, right, bottom, left)
     - `physicalTouchSlop` -> `physical_touch_slop`
     - `displayFeatures*` -> `display_features_{count,bounds,type,state}`
     - `physicalDisplayCornerRadius*` -> `physical_display_corner_radius_*` (top-left, top-right, bottom-right, bottom-left)
   - Result: Passed. Zero dropped parameters.

3. **Falsification Attempt 3: Host test coverage completeness.**
   - Verified plan includes validation testing (negative padding, dimension bounds, buffer nullness when count > 0).
   - Verified plan includes parity event dispatch with non-default values and legacy struct size verification.
   - Result: Passed. Test coverage plan is comprehensive.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-1.8
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-1.8.plan-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   Window metrics extension plan completely bridges the T-0.4 parity contract gaps in FlutterWindowMetricsEvent, preserves C-ABI stability via SAFE_ACCESS, and plans robust validation and backwards-compatibility testing.
NEXT:      implementer
```
