# Implementation Plan: T-1.18c — Dynamic Thread Merging Unit Tests

- **Task:** T-1.18c
- **Branch:** `android-embedder-v8/t-1.18c-thread-merging-tests`
- **Depends on:** T-1.18b (`b3a4262f4e0`)
- **Author:** Matt Boetger / Assistant
- **Date:** 2026-09-16

---

## 1. Goal & Observable Behaviour Change
- **Goal:** Verify dynamic thread merging behavior, error validation, and lifecycle through the public Embedder API in `embedder_unittests.cc`.
- **Observable Behaviour Change:** None in production runtime; adds host unit test coverage for `FlutterCompositor` dynamic thread merging fields and `FlutterRasterThreadMerger*` C-API functions.

---

## 2. Files to be Touched
1. `engine/src/flutter/shell/platform/embedder/tests/embedder_unittests.cc`:
   - Add `DynamicThreadMergingNullCallbackError` test verifying `kInvalidArguments` when `supports_dynamic_thread_merging = true` and `post_preroll_callback = nullptr`.
   - Add `DynamicThreadMergingLifecycle` test driving merge, lease extension, retry, resubmit, success, and frame lifecycle callbacks.

---

## 3. Implementation Sequence
1. **Add Unit Tests (`embedder_unittests.cc`):**
   - Implement `DynamicThreadMergingNullCallbackError`.
   - Implement `DynamicThreadMergingLifecycle`.
2. **Build & Execute Tests:**
   - Compile `embedder_unittests`.
   - Run `./out/host_debug_unopt/embedder_unittests --gtest_filter="*DynamicThreadMerging*"`.
   - Run full `./out/host_debug_unopt/embedder_unittests` to ensure no regressions.
3. **Formatting & Static Analysis:**
   - Format with `ci/format.sh --fix`.
   - Run `migration_verify.dart --task T-1.18c --gates analysis,ratchet`.

---

## 4. Test Plan (Invariant I-4)
- Test 1: `EmbedderTest.DynamicThreadMergingNullCallbackError`:
  - Asserts `FlutterEngineInitialize` fails with `kInvalidArguments`.
- Test 2: `EmbedderTest.DynamicThreadMergingLifecycle`:
  - Asserts `begin_frame_callback`, `post_preroll_callback`, and `end_frame_callback` receive non-null `FlutterFrameThreadingInfo` and non-null `thread_merger`.
  - Asserts `FlutterRasterThreadMergerIsMerged` starts false.
  - Asserts `MergeWithLease` and `ExtendLeaseTo` function properly.
  - Asserts all 3 enum values of `FlutterPostPrerollResult` are exercised without crashing or deadlocking.
  - Asserts presentation callback is reached and latch is signaled.
