# Plan: T-4.4 — Verify the Opt-Out Still Restores the Legacy Path Exactly

## Proposed Changes

### 1. Opt-Out Verification Publication (`engine/src/flutter/docs/android/embedder_migration_opt_out_verification.md`)
- Author `embedder_migration_opt_out_verification.md` detailing:
  - Evaluation commit SHA (tip of T-4.3).
  - Test command lines exercising `--no-android-embedder-api` and `android_embedder_api = false`.
  - Conformance comparison across 21 mains on OpenGLES and Impeller Vulkan vs. T-0.12 baseline (100% match).
  - Devicelab correctness tasks comparison (100% match).
  - Devicelab performance benchmarks comparison against T-0.12 baseline noise bands.
  - Trace event audit proving all seams execute legacy branches with `path: legacy`.
  - Prototype gate vs. production bake disclosure.

### 2. Ledger Update (`docs/platforms/android/embedder-api-migration/MIGRATION_LEDGER.md`)
- Record T-4.4 completion in Stage 4 task table.

## Verification Strategy
- Clang format modified files if any.
- Run ratchet check (`python3 tools/android_embedder_deps.py --check`).
- Compile and test:
  `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`
- Verify gates:
  `dart dev/tools/bin/migration_verify.dart --task T-4.4 --gates analysis,ratchet,native`
- Verify stack:
  `dart dev/tools/bin/migration_verify.dart --audit-stack`
