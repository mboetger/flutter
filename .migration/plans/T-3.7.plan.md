# Plan: T-3.7 — Replace `Shell::Create` / `ThreadHost` with FlutterEngine Lifecycle Seams behind Flag

## Proposed Changes

### 1. AndroidShellHolder Declaration (`android_shell_holder.h`)
- Add public methods:
  ```cpp
  FlutterEngineResult InitializeEngine();
  FlutterEngineResult RunEngine(
      const std::string& entrypoint = "",
      const std::string& library_url = "",
      const std::vector<std::string>& entrypoint_args = {},
      int64_t engine_id = 0);
  ```

### 2. AndroidShellHolder Implementation (`android_shell_holder.cc`)
- Update constructor:
  ```cpp
  if (settings_.android_embedder_api) {
    TRACE_EVENT1("flutter", "AndroidShellHolder::Initialize", "path",
                 "embedder_api");
    InitializeEngine();
    return;
  }

  TRACE_EVENT1("flutter", "AndroidShellHolder::Initialize", "path",
               "legacy");
  // [existing legacy Shell::Create logic]
  ```
- Implement `InitializeEngine()`:
  Extract shell creation and project args initialization into `InitializeEngine()`.
- Update `Launch(...)`:
  ```cpp
  if (settings_.android_embedder_api) {
    TRACE_EVENT1("flutter", "AndroidShellHolder::Launch", "path",
                 "embedder_api");
    RunEngine(entrypoint, libraryUrl, entrypoint_args, engine_id);
    return;
  }

  TRACE_EVENT1("flutter", "AndroidShellHolder::Launch", "path", "legacy");
  // [existing legacy shell_->RunEngine logic]
  ```
- Implement `RunEngine(...)`:
  Build run config, update display metrics, and invoke `shell_->RunEngine`.

### 3. Unit Tests (`android_shell_holder_unittests.cc`)
- Add test `AndroidShellHolder.InitializeEngineLegacyPath`.
- Add test `AndroidShellHolder.InitializeEngineEmbedderApiPath`.
- Add test `AndroidShellHolder.InitializeEngineDirect`.
- Add test `AndroidShellHolder.RunEngineDirect`.

## Verification Strategy
- Clang format modified files.
- `python3 tools/android_embedder_deps.py --check` (<= baseline).
- `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`.
- `dart dev/tools/bin/migration_verify.dart --task T-3.7 --gates analysis,ratchet,native`.
- `dart dev/tools/bin/migration_verify.dart --audit-stack`.
