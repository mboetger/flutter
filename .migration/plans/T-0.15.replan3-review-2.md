# Verification Tooling Plan Review (T-0.15a) — Round 2

**VERDICT:** REVISE
**SPLIT:** justified
**CONTEXT PACK:** sufficient

## Round 1 Findings Disposition
1. **Context pack lacks citations/false Isolate claim**: ACCEPTED-RESOLVED. The rewritten context pack supplies correct `file:line` identifiers (e.g., `pubspec.yaml` L7), `grep` verification commands, and correctly reports via an empty `grep` array that `Isolate.run()` is absent natively. Spot-checked three §G commands (`git show --stat`, `git grep usesVirtualDisplay`, `git grep SupportsDynamicThreadMerging`); all execute as transcribed.
2. **Req 3 missing from T-0.15a (Artifact Integrity)**: ACCEPTED-RESOLVED. The split matrix correctly positions Req 3 within T-0.15a. Step 3 for T-0.15a explicitly includes `"ran": false` generating schema blocks, enabling the branch to emit a valid §A.6 artifact independently.
3. **Flawed B2 Env Test**: ACCEPTED-RESOLVED. The test matrix now instructs executing `/usr/bin/env` via absolute path and actively parsing for `PATH=` natively. This properly enforces inherited environment observation rather than vacuously observing exit statuses.
4. **Missing mandate for B1 CLI fix**: ACCEPTED-RESOLVED. T-0.15a step 4 explicitly specifies `addFlag('check-artifact-sha')`, preventing the parameter from vacuously ingesting `--task <id>` boundaries, correctly closing the validation loop.
5. **DR-0013 path vulnerabilities**: ACCEPTED-RESOLVED (Now DR-0014). Replaced with explicit bounds isolating `R` (Rename) markers natively validating structural `.startsWith('.migration/')` protections against path injections such as `lib/.migration/hack.cc`.
6. **Tests dropped for budgets**: ACCEPTED-RESOLVED (Partially). Tests for Req 1 and Req 9 mutual limits are restored. (See blocking finding underneath regarding a newly introduced test flaw).
7. **Phantom Ledger updates**: ACCEPTED-RESOLVED. T-0.15a step 7 specifically tasks the implementer with amending `MIGRATION_LEDGER.md` (and `.migration/README.md`) to strip documentation strings advertising `--allow-dirty`, `--audit-stack`, etc.

## Split Assessment: justified
The four PR branching sequences efficiently isolate parsing constructs, payload definitions, and structural validations exclusively matching exactly one testable capability per branch bounding. T-0.15a establishes immediate provenance loops without cross-dependency.

## DR-0014 Ruling
- **Can it be gamed?** A malicious subagent attempting to establish untracked code within `.migration/` will succeed in marking it as "tolerable dirt." However, the execution vectors are logged to the JSON telemetry strictly; the file operates inertly. External symlinks trace canonically via git index structures detecting mutations outside, while `..` escapes are normalized.
- **Fails CLOSED?** Yes. Rename (`R`) parsing specifically tests for destination bounds rejecting manipulations.

## BLOCKING Findings

1. **Both Runners Test Dropped (Standing Checks Violation)**: The original plan verified that testing frameworks seamlessly passed `dart test` and `bin/flutter test` explicitly. The new text completely removes all mention of executing tests beneath `bin/flutter test` and does not include it as a testing parameter requirement. This violates the standing checks defined by CI constraints (CI utilizes `flutter test` via `run_framework_tests.dart:307`). The requirement that tests must explicitly pass under both runners natively must be restored.
2. **Req 9 Test Missing Evaluation Clause (I-4 Violation)**: The instruction matrix demands that *each* test state whether it fails against current code natively. The third test mandated for Req 9 (`test('Req 9: fails if code-review-*.md artifacts are missing')`: Asserts missing markdown artifacts fail tool) completely omits defining whether this condition passes or fails against current code.
3. **Req 1 Missing `parent_sha` validation (Requirement Thinning)**: The ledger explicitly mandates: "Always record `commit_sha` and `parent_sha` from git rev-parse." The plan rewrote Req 1 and strictly limits data extraction and testing solely to `commit_sha` bounds (e.g. `test('Req 1: extracts commit_sha from git natively')`). Throttling properties out of the requirement breaks `--audit-stack` (Req 5) validation capabilities which rely natively on the `parent_sha` marker. Both properties must be explicitly requested and tested natively inside Req 1 bounds.

## NON-BLOCKING Observations
- **Line Budget Constraint**: You evaluate ~395 exact lines for T-0.15a, which bounds right below the 400-line I-7 max cap securely. Exercise caution inside JSON templating rendering outputs limiting boilerplate sprawl.

ROLE: PLAN REVIEWER
TASK: T-0.15a
ROUND: 2
VERDICT: REVISE
ARTIFACT: .migration/plans/T-0.15.replan3-review-2.md
BLOCKING: 3
SUMMARY: The rewritten context pack is sufficient with correct citations and validated Isolate.run facts. Round 1 findings surrounding Req 3, DR-0014, B1 parsing, and B2 environmental vulnerabilities were handled solidly closing previous gaps correctly. However, the rewrite silently dropped the CI requirement that tests pass under both 'dart test' and 'bin/flutter test' violating standing checks natively. It additionally thinned Req 1 to only validate commit_sha (dropping parent_sha), and failed to define whether the third Req 9 test fails against current code.
NEXT: PLANNER MUST restore the "both runners" (flutter test and dart test) parity constraint, fully reinstate parent_sha tracking inside Req 1 including its test evaluations, and specify current code failures for the missing Req 9 test clause.
DECISIONS: none
