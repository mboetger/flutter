# Plan: T-3.5 — Replace `platform_view_->SetAccessibilityFeatures(flags)` with `FlutterEngineUpdateAccessibilityFeatures` behind Flag

## Proposed Changes

### 1. PlatformViewAndroid Declaration (`platform_view_android.h`)
- Add public method:
  ```cpp
  FlutterEngineResult UpdateAccessibilityFeatures(
      FlutterAccessibilityFeature features);
  ```

### 2. PlatformViewAndroid Implementation (`platform_view_android.cc`)
- Update `SetAccessibilityFeatures(int32_t flags)`:
  ```cpp
  void PlatformViewAndroid::SetAccessibilityFeatures(int32_t flags) {
    if (android_embedder_api_) {
      TRACE_EVENT1("flutter", "PlatformViewAndroid::SetAccessibilityFeatures",
                   "path", "embedder_api");
      UpdateAccessibilityFeatures(
          static_cast<FlutterAccessibilityFeature>(flags));
      return;
    }

    TRACE_EVENT1("flutter", "PlatformViewAndroid::SetAccessibilityFeatures",
                 "path", "legacy");
    if (platform_view_) {
      platform_view_->SetAccessibilityFeatures(flags);
    }
  }
  ```
- Implement `UpdateAccessibilityFeatures(FlutterAccessibilityFeature features)`:
  ```cpp
  FlutterEngineResult PlatformViewAndroid::UpdateAccessibilityFeatures(
      FlutterAccessibilityFeature features) {
    if (platform_view_) {
      platform_view_->SetAccessibilityFeatures(static_cast<int32_t>(features));
    }
    return kSuccess;
  }
  ```

### 3. Unit Tests (`platform_view_android_unittests.cc`)
- Update `FakePlatformViewDelegate`:
  - `bool set_accessibility_features_called = false;`
  - `int32_t last_accessibility_features = 0;`
- Add test `PlatformViewAndroidTest.AccessibilityFeaturesDispatchLegacyPath`.
- Add test `PlatformViewAndroidTest.AccessibilityFeaturesDispatchEmbedderApiPath`.
- Add test `PlatformViewAndroidTest.UpdateAccessibilityFeaturesDirect`.

## Verification Strategy
- Clang format modified files.
- `python3 tools/android_embedder_deps.py --check` (<= baseline).
- `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`.
- `dart dev/tools/bin/migration_verify.dart --task T-3.5 --gates analysis,ratchet,native`.
- `dart dev/tools/bin/migration_verify.dart --audit-stack`.
