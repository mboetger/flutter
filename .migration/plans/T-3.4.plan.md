# Plan: T-3.4 — Replace `platform_view_->SetSemanticsEnabled(enabled)` with `FlutterEngineUpdateSemanticsEnabled` behind Flag

## Proposed Changes

### 1. PlatformViewAndroid Declaration (`platform_view_android.h`)
- Add public method:
  ```cpp
  FlutterEngineResult UpdateSemanticsEnabled(bool enabled);
  ```

### 2. PlatformViewAndroid Implementation (`platform_view_android.cc`)
- Update `SetSemanticsEnabled(bool enabled)`:
  ```cpp
  void PlatformViewAndroid::SetSemanticsEnabled(bool enabled) {
    if (android_embedder_api_) {
      TRACE_EVENT1("flutter", "PlatformViewAndroid::SetSemanticsEnabled", "path",
                   "embedder_api");
      UpdateSemanticsEnabled(enabled);
      return;
    }

    TRACE_EVENT1("flutter", "PlatformViewAndroid::SetSemanticsEnabled", "path",
                 "legacy");
    if (platform_view_) {
      platform_view_->SetSemanticsEnabled(enabled);
    }
  }
  ```
- Implement `UpdateSemanticsEnabled(bool enabled)`:
  ```cpp
  FlutterEngineResult PlatformViewAndroid::UpdateSemanticsEnabled(bool enabled) {
    if (platform_view_) {
      platform_view_->SetSemanticsEnabled(enabled);
    }
    return kSuccess;
  }
  ```

### 3. Unit Tests (`platform_view_android_unittests.cc`)
- Update `FakePlatformViewDelegate`:
  - `bool set_semantics_enabled_called = false;`
  - `bool last_semantics_enabled = false;`
- Add test `PlatformViewAndroidTest.SemanticsEnabledDispatchLegacyPath`.
- Add test `PlatformViewAndroidTest.SemanticsEnabledDispatchEmbedderApiPath`.
- Add test `PlatformViewAndroidTest.UpdateSemanticsEnabledDirect`.

## Verification Strategy
- Clang format modified files.
- `python3 tools/android_embedder_deps.py --check` (<= baseline).
- `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`.
- `dart dev/tools/bin/migration_verify.dart --task T-3.4 --gates analysis,ratchet,native`.
- `dart dev/tools/bin/migration_verify.dart --audit-stack`.
