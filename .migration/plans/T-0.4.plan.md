# T-0.4 Plan: Viewport Metrics and Platform Message Affinity Characterization

## Goal
Implement characterization tests for:
1. Viewport metrics parameter fidelity: verify that each parameter passed to `FlutterJNI.nativeSetViewportMetrics` arrives intact in `flutter::ViewportMetrics` with 1:1 fidelity.
2. Platform message thread affinity: assert that `PlatformMessageHandlerAndroid::DoesHandlePlatformMessageOnPlatformThread()` returns `false` (in contrast to the Embedder API default `true`), and characterize message delivery thread behaviors.
3. Establish `// PARITY CONTRACT:` comment blocks listing every covered field for downstream use in T-1.8 and T-0.8.

## Files Touched
- `engine/src/flutter/shell/platform/android/android_viewport_metrics_unittests.cc` (new)
- `engine/src/flutter/shell/platform/android/platform_message_affinity_unittests.cc` (new)
- `engine/src/flutter/shell/platform/android/BUILD.gn` (add both files to `flutter_shell_native_unittests`)

## Implementation Sequence
1. Create `android_viewport_metrics_unittests.cc`:
   - Include `// PARITY CONTRACT:` listing all 30 parameters.
   - For each parameter: assert that a distinct non-default value correctly populates the corresponding field in `flutter::ViewportMetrics` (one assertion per parameter).
   - Test display features arrays (bounds, types, states).
   - Test display corner radii (top-left, top-right, bottom-right, bottom-left).
2. Create `platform_message_affinity_unittests.cc`:
   - Include `// PARITY CONTRACT:` documenting Blocker B-3 affinity divergence.
   - Assert `DoesHandlePlatformMessageOnPlatformThread() == false`.
   - Assert thread affinity for incoming message handling and response completion.
3. Wire both tests into `flutter_shell_native_unittests` in `shell/platform/android/BUILD.gn`.
4. Run formatting and verification checks.

## Invariant Checklist
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (establishes Stage 1 parity contracts)
- [x] I-5 Test targets wired (`flutter_shell_native_unittests`)
- [x] I-6 Net coverage preserved (net increase)
- [x] I-7 Tiered diff cap respected (tests excluded from line count)
- [x] I-8 No nogncheck without bug
- [x] I-9 No shared-code changes
- [x] I-10 No Android in shared embedder code
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Decisions
None required. Fully aligned with MIGRATION_LEDGER.md §T-0.4.
