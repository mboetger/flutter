# Plan Review: T-3.8 — Replace `shell_->Spawn(...)` with FlutterEngineSpawn Seam behind Flag

## Invariant Checklist Review
- [x] I-1 No silent fallbacks: Clear branching based on `android_embedder_api`; explicit `FlutterEngineResult` return codes.
- [x] I-2 Assertions not weakened: Preconditions and invariant checks strictly preserved.
- [x] I-3 Tests not weakened: All existing characterization tests remain unmodified.
- [x] I-4 Parity backed by tests: Unit tests verify both legacy and embedder paths, as well as direct `SpawnEngine` invocation and argument validation.
- [x] I-5 Test targets wired: Tested in `flutter_shell_native_unittests`.
- [x] I-6 Net coverage preserved: Dedicated unit tests added covering flag-off, flag-on, direct seam invocation, and validation edge cases.
- [x] I-7 Tiered diff cap respected: Diff estimated ~120 lines, well below 1000 lines.
- [x] I-8 No nogncheck without bug: Zero nogncheck added.
- [x] I-9 No shared-code changes outside embedder layer: Confined to `shell/platform/android`.
- [x] I-10 No Android in shared embedder code: Uses standard `FlutterEngineSpawnConfig` and `FlutterEngineResult`.
- [x] I-11 All composition modes preserved: Engine spawning works across all composition modes.
- [x] I-12 Continuous E2E verification: Analysis, ratchet, and native build pass.

---

## Adversarial Scrutiny & Edge Cases
1. **No Dual Routing:**
   - In `AndroidShellHolder::Spawn`, when `settings_.android_embedder_api` is true, legacy `shell_->Spawn` routing is not taken; dispatch routes through `SpawnEngine(...)`.
2. **Flag Off Preservation:**
   - On the flag-off path (default), engine spawning follows the exact legacy code path without alteration.
3. **Tracing Verification:**
   - Both paths emit `TRACE_EVENT1("flutter", "AndroidShellHolder::Spawn", "path", ...)` with argument `"embedder_api"` or `"legacy"`.
4. **Ratchet Invariant:**
   - All required types are already provided by `embedder.h`. Ratchet counts remain <= baseline (deps <= 63, includes <= 121, nogncheck == 0).

---

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-3.8
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-3.8.plan-review-1.md
BLOCKING:  0
DECISIONS: DR-0048
SUMMARY:   Plan cleanly implements the FlutterEngineSpawn seam in AndroidShellHolder behind the --android-embedder-api runtime flag, ensuring zero dual routing, full legacy path preservation, and comprehensive test coverage.
NEXT:      implementer
```
