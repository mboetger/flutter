# Plan: T-3.6 — Replace `UnregisterTexture` / `MarkTextureFrameAvailable` with Embedder API Seams behind Flag

## Proposed Changes

### 1. PlatformViewAndroid Declaration (`platform_view_android.h`)
- Add public methods:
  ```cpp
  FlutterEngineResult UnregisterExternalTexture(int64_t texture_id);
  FlutterEngineResult MarkExternalTextureFrameAvailable(int64_t texture_id);
  ```

### 2. PlatformViewAndroid Implementation (`platform_view_android.cc`)
- Update `UnregisterTexture(int64_t texture_id)`:
  ```cpp
  void PlatformViewAndroid::UnregisterTexture(int64_t texture_id) {
    if (android_embedder_api_) {
      TRACE_EVENT1("flutter", "PlatformViewAndroid::UnregisterTexture", "path",
                   "embedder_api");
      UnregisterExternalTexture(texture_id);
      return;
    }

    TRACE_EVENT1("flutter", "PlatformViewAndroid::UnregisterTexture", "path",
                 "legacy");
    if (external_texture_adapter_) {
      external_texture_adapter_->UnregisterExternalTexture(texture_id);
      return;
    }
    if (platform_view_) {
      platform_view_->UnregisterTexture(texture_id);
    }
  }
  ```
- Implement `UnregisterExternalTexture(int64_t texture_id)`:
  ```cpp
  FlutterEngineResult PlatformViewAndroid::UnregisterExternalTexture(
      int64_t texture_id) {
    if (texture_id <= 0) {
      return kInvalidArguments;
    }
    if (external_texture_adapter_) {
      return external_texture_adapter_->UnregisterExternalTexture(texture_id);
    }
    if (platform_view_) {
      platform_view_->UnregisterTexture(texture_id);
    }
    return kSuccess;
  }
  ```
- Update `MarkTextureFrameAvailable(int64_t texture_id)`:
  ```cpp
  void PlatformViewAndroid::MarkTextureFrameAvailable(int64_t texture_id) {
    if (android_embedder_api_) {
      TRACE_EVENT1("flutter", "PlatformViewAndroid::MarkTextureFrameAvailable",
                   "path", "embedder_api");
      MarkExternalTextureFrameAvailable(texture_id);
      return;
    }

    TRACE_EVENT1("flutter", "PlatformViewAndroid::MarkTextureFrameAvailable",
                 "path", "legacy");
    if (external_texture_adapter_) {
      external_texture_adapter_->MarkExternalTextureFrameAvailable(texture_id);
      return;
    }
    if (platform_view_) {
      platform_view_->MarkTextureFrameAvailable(texture_id);
    }
  }
  ```
- Implement `MarkExternalTextureFrameAvailable(int64_t texture_id)`:
  ```cpp
  FlutterEngineResult PlatformViewAndroid::MarkExternalTextureFrameAvailable(
      int64_t texture_id) {
    if (texture_id <= 0) {
      return kInvalidArguments;
    }
    if (external_texture_adapter_) {
      return external_texture_adapter_->MarkExternalTextureFrameAvailable(
          texture_id);
    }
    if (platform_view_) {
      platform_view_->MarkTextureFrameAvailable(texture_id);
    }
    return kSuccess;
  }
  ```

### 3. Unit Tests (`platform_view_android_unittests.cc`)
- Update `FakePlatformViewDelegate`:
  - `bool unregister_texture_called = false;`
  - `int64_t last_unregistered_texture_id = -1;`
  - `bool mark_texture_frame_available_called = false;`
  - `int64_t last_frame_available_texture_id = -1;`
- Add test `PlatformViewAndroidTest.UnregisterTextureDispatchLegacyPath`.
- Add test `PlatformViewAndroidTest.UnregisterTextureDispatchEmbedderApiPath`.
- Add test `PlatformViewAndroidTest.UnregisterExternalTextureDirect`.
- Add test `PlatformViewAndroidTest.MarkTextureFrameAvailableDispatchLegacyPath`.
- Add test `PlatformViewAndroidTest.MarkTextureFrameAvailableDispatchEmbedderApiPath`.
- Add test `PlatformViewAndroidTest.MarkExternalTextureFrameAvailableDirect`.
- Add test `PlatformViewAndroidTest.TextureSeamInvalidArguments`.

## Verification Strategy
- Clang format modified files.
- `python3 tools/android_embedder_deps.py --check` (<= baseline).
- `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`.
- `dart dev/tools/bin/migration_verify.dart --task T-3.6 --gates analysis,ratchet,native`.
- `dart dev/tools/bin/migration_verify.dart --audit-stack`.
