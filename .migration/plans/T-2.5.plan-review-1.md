# Plan Review: T-2.5 — Extract Thread Configuration into Standalone Unit Matching FlutterCustomTaskRunners Shape

## Invariant Checklist Review
- [x] I-1 No silent fallbacks: Thread priorities, thread mask logic, and thread names match existing behavior exactly.
- [x] I-2 Assertions not weakened: All thread assertions, DCHECKs, and message loop checks retained.
- [x] I-3 Tests not weakened: Stage 0 characterization tests in `android_threading_unittests.cc` run unmodified.
- [x] I-4 Parity backed by tests: Stage 3 parity contract constants verified in unit tests.
- [x] I-5 Test targets wired: Built and verified via `flutter_shell_native_unittests`.
- [x] I-6 Net coverage preserved: Additional unit tests cover `AndroidTaskRunners` directly.
- [x] I-7 Tiered diff cap respected: Diff estimated at ~250 lines, well below the 1000-line limit.
- [x] I-8 No nogncheck without bug: Zero nogncheck comments introduced.
- [x] I-9 No shared-code changes outside embedder layer: All changes strictly isolated to `shell/platform/android`.
- [x] I-10 No Android in shared embedder code: Uses standard public types from `embedder.h`.
- [x] I-11 All composition modes preserved: No alterations to composition, vsync, or rendering threads.
- [x] I-12 Continuous E2E verification: Will pass analysis and ratchet checks.

---

## Adversarial Scrutiny & Edge Cases
1. **Thread Priority Setter Signature & Behavior:**
   - `AndroidPlatformThreadPrioritySetter` matches `void (*)(FlutterThreadPriority)`.
   - Maps `kBackground` to 10 (efficiency cores), `kDisplay` to -1 (display priority), `kRaster` to -5 with -2 fallback, and `kNormal` to 0 (default priority).
   - This exactly matches the priority constants documented in T-0.3.
2. **Dynamic Thread Merging Compatibility:**
   - Merged platform/UI thread option (`settings.merged_platform_ui_thread`) is properly evaluated during mask creation. When enabled, UI runner is assigned to platform runner.
3. **Spawn Sharing:**
   - In `AndroidShellHolder::Spawn()`, reusing `task_runners_` preserves thread host sharing across multiple shell instances without creating extraneous OS threads.
4. **Ratchet Integrity:**
   - Removing `flutter/shell/common/thread_host.h` from `android_shell_holder.{h,cc}` (-2) while adding it only in `android_task_runners.cc` (+1) yields a net reduction of 1 internal include.

---

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-2.5
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-2.5.plan-review-1.md
BLOCKING:  0
DECISIONS: DR-0034
SUMMARY:   Plan cleanly extracts thread configuration from AndroidShellHolder into AndroidTaskRunners, exposes both flutter::TaskRunners and FlutterCustomTaskRunners shapes, preserves exact Android thread priorities and affinities, and reduces internal header includes.
NEXT:      implementer
```
