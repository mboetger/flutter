# Plan Review: T-4.1 — Default --android-embedder-api to On in the Tree

## Invariant Checklist Review
- [x] I-1 No silent fallbacks: Flipping the default leaves the opt-out mechanism explicit and verified.
- [x] I-2 Assertions not weakened: Preconditions and invariant checks strictly preserved.
- [x] I-3 Tests not weakened: All Stage 0 characterization tests remain unmodified.
- [x] I-4 Parity backed by tests: Unit tests verify both default-on behavior and explicit opt-out via `--no-android-embedder-api` or `android_embedder_api = false`.
- [x] I-5 Test targets wired: Tested in `flutter_shell_native_unittests` and `switches_unittests`.
- [x] I-6 Net coverage preserved: Existing unit tests updated to verify the new default and opt-out paths.
- [x] I-7 Tiered diff cap respected: Minimal surgical diff across settings, switches, tests, and ledger.
- [x] I-8 No nogncheck without bug: Zero nogncheck added.
- [x] I-9 No shared-code changes outside embedder layer: The flag and its switch are engine-wide common switches established in Stage 3.
- [x] I-10 No Android in shared embedder code: Standard boolean settings flag.
- [x] I-11 All composition modes preserved: All six composition modes advance to `embedder-default`.
- [x] I-12 Continuous E2E verification: Analysis, ratchet, and native build pass.

---

## Adversarial Scrutiny & Edge Cases
1. **Opt-Out Integrity:**
   - Both `--no-android-embedder-api` and `--android-embedder-api=false` must still explicitly set `settings.android_embedder_api = false`.
2. **Settings Default Alignment:**
   - Both `Settings` struct default in `settings.h` and the command-line parser default in `switches.cc` must agree on `true`.
3. **Stage 3 Exit Criteria:**
   - All Stage 3 exit criteria must be documented as complete, and the B.1 tracker must advance all 6 modes to `embedder-default`.
4. **Ratchet Counts:**
   - No new dependencies or includes added. Counts remain deps = 63, includes = 121, nogncheck = 0.

---

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-4.1
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-4.1.plan-review-1.md
BLOCKING:  0
DECISIONS: DR-0055
SUMMARY:   Plan cleanly flips --android-embedder-api to on by default across the tree, preserves the opt-out path, updates B.1 tracker to embedder-default across all modes, and marks Stage 3 complete.
NEXT:      implementer
```
