# Context Pack: T-4.5 — Delete the Flag and All Legacy Paths

## Predecessor Tasks
- **T-4.1:** Default `--android-embedder-api` to on in the tree (`8de2742e5f3`).
- **T-4.2:** Soak runs across 21 integration test mains and Devicelab suites (`763daf76da7`).
- **T-4.3:** Flip public default; flag remains as opt-out (`284bf781ebb`).
- **T-4.4:** Verify opt-out restores legacy path exactly (`5551423f007`).

## Gating Conditions Check (§A.1, §Stage 4)
- **T-0.9 ratchet:** Clean (63 deps, 121 includes, 0 nogncheck).
- **Overrides in google3:** Zero overrides remaining.
- **Defects:** No open P0/P1 attributed to migration.
- **Characterization tests:** Unmodified and passing.
- **nogncheck count:** Zero across `shell/platform/android`.
- **Composition mode tracker B.1:** All 6 modes (`VD`, `TLHC`, `HC`, `HCPP`, `SurfaceTexture`, `SurfaceProducer`) at `embedder-default`.
- **HC status:** `embedder-default`.

## Scope of Changes
- Engine switches: Remove `kAndroidEmbedderApi` and `kNoAndroidEmbedderApi`.
- Engine settings: Remove `settings.android_embedder_api`.
- `AndroidShellHolder`: Unconditionally route initialization, launch, and spawn through embedder API. Remove `IsAndroidEmbedderApiEnabled`.
- `PlatformViewAndroid`: Unconditionally route pointer, viewport, semantics, platform messages, external textures, and composition through embedder API methods. Remove `android_embedder_api_` field and `IsAndroidEmbedderApiEnabled`.
- Unit tests: Remove switch tests, flag tests, and legacy path tests.
