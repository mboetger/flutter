# T-1.5 Plan Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (host unit tests verify each field's round-trip fidelity)
- [x] I-5 Test targets wired (`embedder_a11y_unittests.cc` already built and run in GN)
- [x] I-6 Net coverage preserved (new tests added covering all 10 fields with both default and non-default values)
- [x] I-7 Tiered diff cap respected (~150 lines)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (only `embedder.h`, `embedder_semantics_update.cc`, `embedder_a11y_unittests.cc`)
- [x] I-10 No Android in shared embedder code (all changes in embedder API and semantics update adapter, zero Android dependencies)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Completeness of the unmapped field list.**
   - Compared every field of `flutter::SemanticsNode` (`engine/src/flutter/lib/ui/semantics/semantics_node.h`) against `FlutterSemanticsNode2` (`embedder.h:1697-1803`).
   - Verified that exactly 10 fields were absent: `maxValueLength`, `currentValueLength`, `traversalParent`, `minValue`, `maxValue`, `hitTestTransform`, `linkUrl`, `role`, `validationResult`, `locale`.
   - Verified that all other fields already exist in `FlutterSemanticsNode2`.
   - Result: Passed. Authoritative mapping established.

2. **Falsification Attempt 2: C-ABI backward compatibility rules (§6.1).**
   - Verified all 10 fields are appended to the end of `FlutterSemanticsNode2`.
   - Verified `FLUTTER_ENGINE_VERSION` is preserved at 1 per §G.5 and §G.9 Item 2 (known trap confirmed: forward compatibility is `struct_size` + `SAFE_ACCESS`).
   - Verified string lifetimes are explicitly documented as valid for the duration of the callback.
   - Result: Passed. Strict C-ABI rules respected.

3. **Falsification Attempt 3: Host test coverage completeness.**
   - Verified plan requires test verifying every field individually with distinct non-default values and default values.
   - Result: Passed. Comprehensive test coverage planned.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-1.5
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-1.5.plan-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   Semantics node field gap plan correctly identifies all 10 missing fields, adheres strictly to C-ABI append-only rules and §G.5 versioning invariant, and provides comprehensive host testing.
NEXT:      implementer
```
