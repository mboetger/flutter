# Plan Review: T-3.4 — Replace `platform_view_->SetSemanticsEnabled(enabled)` with `FlutterEngineUpdateSemanticsEnabled` behind Flag

## Invariant Checklist Review
- [x] I-1 No silent fallbacks: Clear branching based on `android_embedder_api_`; explicit `FlutterEngineResult` return codes.
- [x] I-2 Assertions not weakened: Preconditions strictly preserved.
- [x] I-3 Tests not weakened: All Stage 0 characterization tests remain unmodified.
- [x] I-4 Parity backed by tests: Unit tests verify both legacy and embedder paths, including assertion that legacy delegate receives state.
- [x] I-5 Test targets wired: Tested in `flutter_shell_native_unittests`.
- [x] I-6 Net coverage preserved: Dedicated unit tests added covering flag-off, flag-on, and direct seam invocation.
- [x] I-7 Tiered diff cap respected: Diff estimated ~90 lines, well below 1000 lines.
- [x] I-8 No nogncheck without bug: Zero nogncheck added.
- [x] I-9 No shared-code changes outside embedder layer: Confined to `shell/platform/android`.
- [x] I-10 No Android in shared embedder code: Uses platform-agnostic `FlutterEngineResult`.
- [x] I-11 All composition modes preserved: Semantics enabled dispatch works across all composition modes.
- [x] I-12 Continuous E2E verification: Analysis, ratchet, and native build pass.

---

## Adversarial Scrutiny & Edge Cases
1. **No Dual Routing:**
   - In `SetSemanticsEnabled`, when `android_embedder_api_` is true, the direct `platform_view_->SetSemanticsEnabled(enabled)` call is NOT executed. Dispatch routes exclusively through `UpdateSemanticsEnabled(enabled)`.
2. **Flag Off Preservation:**
   - On the flag-off path (default), incoming calls forward directly to `platform_view_->SetSemanticsEnabled(enabled)` with zero changes.
3. **Tracing Verification:**
   - Both paths emit `TRACE_EVENT1("flutter", "PlatformViewAndroid::SetSemanticsEnabled", "path", ...)`, ensuring Perfetto traces can differentiate paths.
4. **Ratchet Invariant:**
   - All types (`FlutterEngineResult`) are already available via `embedder.h`. Ratchet counts remain <= baseline (deps <= 63, includes <= 121, nogncheck == 0).

---

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-3.4
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-3.4.plan-review-1.md
BLOCKING:  0
DECISIONS: DR-0044
SUMMARY:   Plan cleanly implements the FlutterEngineUpdateSemanticsEnabled C-ABI seam in PlatformViewAndroid behind the --android-embedder-api runtime flag, ensuring zero dual routing, full legacy path preservation, and comprehensive test coverage.
NEXT:      implementer
```
