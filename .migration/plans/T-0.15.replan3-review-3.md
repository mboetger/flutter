# Verification Tooling Plan Review (T-0.15a) — Round 3

**VERDICT:** REVISE
**SPLIT:** justified
**CONTEXT PACK:** sufficient

## Round 2 Findings Disposition

1. **Both-runners constraint**: ACCEPTED-RESOLVED. The plan requires tests to pass under `dart test` and `flutter test` in the preamble and step 1 of every branch.
2. **`parent_sha`**: ACCEPTED-RESOLVED. Requirement 1 limits extraction to git execution and forbids CLI ingestion. A named test verifies both SHAs.
3. **Req 9 third test**: ACCEPTED-DISPUTED. The definition states the test fails against current code. This is false. I read `dev/tools/lib/migration_verify.dart`; lines 218-223 already check for missing review files (`if (!foundCodeReview) { ... return 1; }`). The test passes against current code.

## Conservation Pass

Comparing the current plan to the inherited round 1 plan (`T-0.15.plan.old.md`):

- **§A.7 preconditions**: The old plan required `--device-lock` to "evaluate all §A.7 preconditions". The current text drops this.
- **Hard cleanup**: The old plan required `adb uninstall` and `adb logcat -c` before lock release. The current text drops this.
- **CLI arguments**: The old plan instructed implementing `--task` and `--gates`. The current text drops these.

None of these removals have stated reasons.

## Full Re-verification & Blocking Findings

1. **Missing End-to-End Self-Verification Test**: The instructions require a named end-to-end test proving T-0.15a produces a valid §A.6-compliant artifact for its own branch. The plan defines piecemeal unit tests but omits a full end-to-end test.
2. **Conservation Failure: §A.7 Preconditions**: The requirement to evaluate device preconditions (SDK version, `dumpsys deviceidle`) was dropped from T-0.15d. An implementer taking the cheapest path will omit device validation.
3. **Conservation Failure: Hard Cleanup**: The requirement to run `adb uninstall` and `adb logcat -c` before releasing the lock was dropped from T-0.15d. Without this, tests leak device state.
4. **Conservation Failure: Missing CLI Arguments**: The `--task` and `--gates` arguments were dropped from the implementation steps. The plan must explicitly define them to prevent hardcoding.

ROLE: PLAN REVIEWER
TASK: T-0.15a
ROUND: 3
VERDICT: REVISE
ARTIFACT: .migration/plans/T-0.15.replan3-review-3.md
BLOCKING: 4
SUMMARY: Round 2 findings are checked. The both-runners constraint and parent_sha requirements were rightly restored. The Req 9 test failure state was supplied but is disputed, as the current code already checks for reviews. A conservation pass revealed three omissions from the inherited plan without stated reasons: evaluating §A.7 device preconditions, enforcing hard device cleanup (uninstall/logcat) upon lock release, and explicitly charting the --task and --gates CLI arguments. Additionally, there is no named end-to-end test proving T-0.15a generates a valid artifact for itself.
NEXT: PLANNER MUST restore the dropped constraints (evaluating §A.7 preconditions, adb hard cleanup, and defining --task/--gates) and add a named end-to-end test proving T-0.15a successfully outputs a valid JSON artifact.
DECISIONS: none
