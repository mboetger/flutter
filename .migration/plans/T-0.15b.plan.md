# Plan: T-0.15b — Stack audit and device lock

## Goal
Implement across-branch history checking (`--audit-stack`) and exclusive hardware lock (`--device-lock`).

## Context
- Governed by MIGRATION_LEDGER.md §A.6, §A.7, T-0.15b.
- Refactor `_deviceLock` to use robust non-casting `io.File` handling with guaranteed unlock via `try ... finally`.
- Enforce `_auditStack` validation across all committed `.migration/verification/*.json` artifacts, ensuring each `commit_sha` is present in the branch git history.

## Steps
1. Refactor `_auditStack` in `dev/tools/lib/migration_verify.dart`.
2. Refactor `_deviceLock` in `dev/tools/lib/migration_verify.dart`.
3. Add unit tests in `dev/tools/test/migration_verify_test.dart` covering audit-stack success, audit-stack failure, device-lock missing serial failure.
4. Verify format, analyze, and unit tests.
