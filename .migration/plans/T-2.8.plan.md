# Plan: T-2.8 — Restructure AndroidShellHolder toward FlutterProjectArgs Construction

## 1. Overview
Restructure `AndroidShellHolder` to construct and maintain `FlutterProjectArgs` combining `AndroidTaskRunners` (from T-2.5), `APKAssetProvider` (from T-2.6), `Settings`, and `PlatformViewAndroidJNI` callbacks, matching the Embedder API initialization shape for Stage 3 while maintaining 100% behavioral equivalence in Stage 2.

## 2. Invariants Checked
- [x] I-1 No silent fallbacks: Retains existing `Shell::Create` paths without deviation.
- [x] I-2 Assertions not weakened: Retains all assertions and preconditions.
- [x] I-3 Tests not weakened: All existing characterization and unit tests pass unmodified.
- [x] I-4 Parity proven by test: Parity validated through comprehensive unit tests of `FlutterProjectArgs` contents.
- [x] I-5 Test targets wired: Built and executed in `flutter_shell_native_unittests`.
- [x] I-6 Net coverage preserved: Direct unit tests added for `FlutterProjectArgs` construction and callback execution.
- [x] I-7 Tiered diff cap respected: Diff estimated ~200 lines, well below 1000 lines.
- [x] I-8 No nogncheck without bug: Zero nogncheck added.
- [x] I-9 No shared-code changes outside embedder layer: Confined entirely to `shell/platform/android`.
- [x] I-10 No Android in shared embedder code: Uses platform-agnostic `embedder.h` structures.
- [x] I-11 All composition modes preserved: Composition pipeline unaffected.
- [x] I-12 Continuous E2E verification: Analysis, ratchet, and native build pass.

## 3. Detailed Steps
1. **Update `AndroidShellHolder` (`android_shell_holder.h` and `android_shell_holder.cc`):**
   - Add `#include "flutter/shell/platform/embedder/embedder.h"` in `android_shell_holder.h`.
   - Add members:
     - `FlutterProjectArgs project_args_ = {};`
     - `const FlutterAssetResolver* asset_resolvers_[1] = {nullptr};`
     - `std::vector<const char*> command_line_argv_;`
   - Add static callback trampolines forwarding to `AndroidShellHolder` / `jni_facade_`:
     - `OnPreEngineRestart`
     - `OnSetApplicationLocale`
     - `OnGetScaledFontSize`
     - `OnRequestDartDeferredLibrary`
   - Add helper `void InitializeProjectArgs();` called from constructors.
   - Update `Launch(...)` and `Spawn(...)` to bind `asset_resolvers_[0]` from `apk_asset_provider_->GetFlutterAssetResolver()` and update `engine_id`.
   - Expose:
     - `const FlutterProjectArgs* GetProjectArgsForTesting() const;`
     - `FlutterProjectArgs CreateFlutterProjectArgs(const std::string& entrypoint = "", const std::string& library_url = "", const std::vector<std::string>& entrypoint_args = {}, int64_t engine_id = 0) const;`

2. **Add Unit Tests in `android_shell_holder_unittests.cc`:**
   - Test `ProjectArgsConstruction`: verifies `struct_size`, `custom_task_runners`, callbacks, and asset paths.
   - Test `ProjectArgsWithAssetResolver`: verifies `asset_resolvers_count` and `FlutterAssetResolver` pointer integration.
   - Test `ProjectArgsCallbacksForwardToJNI`: verifies C-ABI callbacks forward properly to `MockPlatformViewAndroidJNI`.

3. **Formatting & Validation:**
   - Run `git add -A` and `./ci/format.sh --fix`.
   - Run `python3 tools/android_embedder_deps.py --check`.
   - Compile and verify with `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`.
