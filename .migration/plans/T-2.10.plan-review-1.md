# Plan Review: T-2.10 — Introduce an External Texture Seam Shaped like FlutterEngineRegisterExternalTexture

## Invariant Checklist Review
- [x] I-1 No silent fallbacks: Both legacy JNI paths (`RegisterSurfaceTexture`, `RegisterImageTexture`) and Embedder API paths (`RegisterExternalTexture`) are fully preserved.
- [x] I-2 Assertions not weakened: Preconditions, null checks, and rendering API checks strictly preserved.
- [x] I-3 Tests not weakened: All existing characterization and unit tests remain unmodified.
- [x] I-4 Parity backed by tests: Unit tests verify both Embedder API shape handling and concrete texture instantiation across rendering APIs.
- [x] I-5 Test targets wired: Built and tested in `flutter_shell_native_unittests`.
- [x] I-6 Net coverage preserved: Dedicated unit tests added in `android_external_texture_adapter_unittests.cc`.
- [x] I-7 Tiered diff cap respected: Diff estimated ~350 lines, well below 1000 lines.
- [x] I-8 No nogncheck without bug: Zero nogncheck added.
- [x] I-9 No shared-code changes outside embedder layer: Confined entirely to `shell/platform/android`.
- [x] I-10 No Android in shared embedder code: Uses platform-agnostic `embedder.h` return codes and types.
- [x] I-11 All composition modes preserved: Zero files under `external_view_embedder/` deleted; neither external texture implementation deleted.
- [x] I-12 Continuous E2E verification: Analysis, ratchet, and native build pass.

---

## Adversarial Scrutiny & Edge Cases
1. **Dual Registration Path Interoperability ("Both paths keep working"):**
   - JNI continues calling `PlatformViewAndroid::RegisterExternalTexture` and `RegisterImageTexture`, which delegate to `AndroidExternalTextureAdapter::RegisterSurfaceTexture` and `RegisterImageTexture`.
   - Embedder-style callers can invoke `AndroidExternalTextureAdapter::RegisterExternalTexture(texture_id)`.
   - Both paths correctly notify the engine texture registrar via `Delegate::OnRegisterTexture`.
   - `UnregisterTexture` and `MarkTextureFrameAvailable` correctly clear internal state and propagate to the delegate.
2. **Rendering API Dispatch Integrity:**
   - All 6 concrete external texture classes (`SurfaceTextureExternalTextureGLSkia`, `SurfaceTextureExternalTextureGLImpeller`, `SurfaceTextureExternalTextureVKImpeller`, `ImageExternalTextureGLSkia`, `ImageExternalTextureGLImpeller`, `ImageExternalTextureVKImpeller`) are instantiated identically according to `android_context_.RenderingApi()`.
   - Neither implementation is deleted or altered (I-11).
3. **Ratchet and Include Hygiene:**
   - Headers used are all in `ALLOWED_HEADER_PREFIXES` (`flutter/common/`, `flutter/shell/platform/android/`, `flutter/shell/platform/embedder/embedder.h`).
   - Ratchet counts remain <= baseline (deps <= 63, includes <= 121, nogncheck == 0).

---

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-2.10
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-2.10.plan-review-1.md
BLOCKING:  0
DECISIONS: DR-0039
SUMMARY:   Plan cleanly introduces AndroidExternalTextureAdapter as an external texture seam in front of SurfaceTextureExternalTexture and ImageExternalTexture, shaped like FlutterEngineRegisterExternalTexture, preserving both registration paths and maintaining all invariants.
NEXT:      implementer
```
