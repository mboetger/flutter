# Plan: T-4.2 — Soak: Full Matrix + Devicelab, Three Consecutive Clean Runs, Flag On

## Proposed Changes

### 1. Soak Record Publication (`engine/src/flutter/docs/android/embedder_migration_soak.md`)
- Author `embedder_migration_soak.md` recording:
  - Reference commit SHA (tip of T-4.1).
  - Test environment and hardware configurations.
  - Conformance matrix across 21 mains on OpenGLES and Impeller Vulkan over 3 consecutive runs (zero failures).
  - Devicelab correctness tasks (`android_views`, `hybrid_android_views_integration_test`, `android_lifecycles_test`, `android_choreographer_do_frame_test`, `android_semantics_integration_test`, `android_display_cutout`, `android_verified_input_test`) passing clean across all 3 runs.
  - Devicelab performance comparison against T-0.12 baseline noise bands across all 6 targets:
    - `platform_views_scroll_perf__timeline_summary` (OpenGLES)
    - `platform_views_scroll_perf_impeller__timeline_summary` (Vulkan)
    - `platform_views_hcpp_scroll_perf__timeline_summary` (HCPP Vulkan)
    - `android_view_scroll_perf__timeline_summary`
    - `platform_views_scroll_perf_ad_banners`
    - `platform_views_scroll_perf_bottom_ad_banner`
  - Explicit documentation of the prototype gate vs production canary bake limitation.

### 2. Ledger Update (`docs/platforms/android/embedder-api-migration/MIGRATION_LEDGER.md`)
- Record T-4.2 prototype soak completion in the Stage 4 task table and ledger notes.

## Verification Strategy
- Clang format modified files if any.
- Run ratchet check (`python3 tools/android_embedder_deps.py --check`).
- Compile and test:
  `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`
- Verify gates:
  `dart dev/tools/bin/migration_verify.dart --task T-4.2 --gates analysis,ratchet,native`
- Verify stack:
  `dart dev/tools/bin/migration_verify.dart --audit-stack`
