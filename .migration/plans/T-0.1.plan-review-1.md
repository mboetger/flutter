# T-0.1 Plan Review 1

## Audit of Context Pack against §F.3
- Ground truth verification confirmed against `shell/platform/android/BUILD.gn` and unit test files.
- Subsystem coverage accurately reflected.
- Historical v7 context and known failure modes accounted for.

## Falsification Attempts
1. **Falsification Attempt 1: Check if any surface lifecycle tests already exist in native unittests.**
   - Inspected `shell/platform/android/platform_view_android_unittests.cc`. It only contains `DISABLED_SelectsVulkanBasedOnApiLevel`.
   - Inspected `shell/platform/android/android_shell_holder_unittests.cc`. It only calls `NotifyCreated(fake_window)` for fixture setup; no tests exist for `NotifySurfaceWindowChanged` or `NotifyDestroyed`.
   - Result: Verified. Native surface lifecycle is completely unexercised.
2. **Falsification Attempt 2: Check if viewport metrics 30 parameters are tested in Robolectric.**
   - Searched `FlutterViewTest.java` and `FlutterJNITest.java` for viewport metrics assertions. While basic insets are touched, there is no assertion verifying that all 30 parameters passed to `FlutterJNI.nativeSetViewportMetrics` reach native code intact.
   - Result: Verified. Viewport metrics fidelity is a critical gap.
3. **Falsification Attempt 3: Check if thread priorities or dynamic thread merging are asserted in any test.**
   - Inspected `android_shell_holder_unittests.cc`. It tests whether `GetUITaskRunner() == GetPlatformTaskRunner()`, but never checks `setpriority()` or `AndroidPlatformThreadConfigSetter`.
   - Inspected `external_view_embedder/` tests. No unit test asserts `SupportsDynamicThreadMerging()`.
   - Result: Verified. Thread priorities and dynamic thread merging are untested.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-0.1
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-0.1.plan-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   Plan is comprehensive, rigorously covers all 12 subsystems, and satisfies all T-0.1 requirements.
NEXT:      implementer
```
