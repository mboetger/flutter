# T-1.7 Plan Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (host unit tests and end-to-end integration tests verify SetApplicationLocale dispatch from Dart to embedder)
- [x] I-5 Test targets wired (`platform_view_embedder_unittests.cc` and `embedder_unittests.cc` wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (new tests covering both unit dispatch and end-to-end Dart runtime invocation)
- [x] I-7 Tiered diff cap respected (~150 lines)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (all changes within `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (platform-agnostic callback with BCP 47 string)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1).**
   - Verified that `FlutterSetApplicationLocaleCallback` is appended to the end of `FlutterProjectArgs`.
   - Verified that `SAFE_ACCESS(args, set_application_locale_callback, nullptr)` is used in `embedder.cc`.
   - Verified that string lifetime is documented as valid for callback duration.
   - Verified that calling thread is documented as the platform task runner thread.
   - Verified that `FLUTTER_ENGINE_VERSION` is preserved (§G.5, §G.9 Item 2).
   - Result: Passed. Strict C-ABI rules respected.

2. **Falsification Attempt 2: Verify dispatch pathway parity with Android and iOS.**
   - Verified that `PlatformViewEmbedder` overrides `SetApplicationLocale(std::string locale)` matching `PlatformViewAndroid` and `PlatformViewIOS`.
   - Verified that `PlatformDispatchTable` safely forwards the call when the callback is set and safely no-ops when null.
   - Result: Passed. Full parity with existing platform views.

3. **Falsification Attempt 3: Host test coverage completeness.**
   - Verified plan includes both unit test in `platform_view_embedder_unittests.cc` and end-to-end test in `embedder_unittests.cc` using a Dart entrypoint calling `PlatformDispatcher.instance.setApplicationLocale`.
   - Result: Passed. Comprehensive test coverage planned.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      plan-reviewer
TASK:      T-1.7
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/plans/T-1.7.plan-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   SetApplicationLocale callback plan adheres strictly to C-ABI rules, bridges the platform view gap in PlatformViewEmbedder, and provides comprehensive unit and end-to-end testing.
NEXT:      implementer
```
