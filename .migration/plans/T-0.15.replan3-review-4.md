# Verification Tooling Plan Review (T-0.15a) — Round 4

**VERDICT:** APPROVE
**SPLIT:** justified (single branch)
**CONTEXT PACK:** sufficient

## Round 3 Findings Disposition

1. **§A.7 device precondition evaluation dropped**: ACCEPTED-RESOLVED. T-0.15a Step 8 restores validating §A.7 preconditions.
2. **Hard device cleanup dropped**: ACCEPTED-RESOLVED. T-0.15a Step 8 dictates running `adb uninstall` and `adb logcat -c` on exit.
3. **`--task` and `--gates` CLI arguments missing**: ACCEPTED-RESOLVED. T-0.15a Step 2 maps parsing for these variables.
4. **No named end-to-end test proving self-verification**: ACCEPTED-RESOLVED. The test matrix now includes an E2E test asserting the tool emits a complete schema-valid output on a dirtied environment, validating `commit_sha` and `parent_sha` retrieved from git.

## Conservation Pass

Comparing the collapsed single-branch plan against the multi-branch structure:
- `--audit-stack` and `--summary` absorbed into T-0.15a Step 7.
- `--device-lock` absorbed into T-0.15a Step 8.
- Smart Workspace constraints (`--allow-dirty`) absorbed into T-0.15a Step 6.
- `--update-goldens` blocked by tests and T-0.15a logic.

No controls or definitions were silently dropped during the branch merge. Every component whose deferral was justified solely by the line budget is now incorporated into the single branch.

## Load-Bearing Constraints Checks

- **T-0.15a Self-Verifying**: Addressed via the E2E test evaluating the tool correctly on a dirtied working branch.
- **Matrix Accounting**: 9 base requirements + Env + Defer + E2E = 12 total listed requirements resulting in 14 tests. All accurately listed and uniquely evaluated.
- **DR-0013 / DR-0014**: Renames (`R`) securely evaluated extracting boundaries guaranteeing `.startswith('.migration/')`.
- **Line Counts**: Tooling additions approximate ~1200 lines, fitting under the DR-0015 `3000` cap. Tests are correctly excluded.
- **Test Runners**: The `flutter test` AND `dart test` parity constraints are reinstated in the preamble and steps.

## Evidence Evaluation / Falsification Attempts

1. **B2 Env Vacuity (Testing inherited environment fails without stdout parsing)**:
   ```bash
   cat << 'EOF' > dev/tools/test/test_env_3.dart
   import 'dart:io';
   import 'package:dev_tools/migration_verify.dart';

   void main() async {
     final executor = RealSystemCommandExecutor();
     final result = await executor.run(['/usr/bin/env'], includeParentEnvironment: false);
     if (result.stdout.contains('PATH=')) {
       print('PASS');
     } else {
       print('FAIL: stdout |${result.stdout}|');
     }
   }
   EOF
   dart run dev/tools/test/test_env_3.dart
   ```
   **Output:** `FAIL: stdout ||`
   **Result:** Proved planner correct; current tool passes an empty environment exiting `0` silently. Asserts must inspect output.

2. **B4 Vacuous Bypass (Testing zero gates)**:
   ```bash
   git stash -u
   dart dev/tools/bin/migration_verify.dart --task T-0.15a --gates ""
   echo "EXIT: $?"
   ```
   **Output:** Emits empty `gates: {}` JSON payload with `EXIT: 0` printed.
   **Result:** Proved planner correct; zero gates executes successfully without validation.

3. **B5 Defer Exceptions (Testing unknown flags)**:
   ```bash
   dart dev/tools/bin/migration_verify.dart --allow-dirty
   ```
   **Output:** `Unhandled exception: FormatException: Could not find an option named "--allow-dirty".`
   **Result:** Proved planner correct; tool crashes rather than cleanly terminating on missing flags.

## NON-BLOCKING Observations
- **Req 8 Prose Inaccuracy**: The matrix states the Req 8 (`--update-goldens`) test fails against current code. This is technically false; `dev/tools/lib/migration_verify.dart` L105 already evaluates this flag returning `1`. The implementer will observe the corresponding test passing immediately without modification.

ROLE: PLAN REVIEWER
TASK: T-0.15a
ROUND: 4
VERDICT: APPROVE
ARTIFACT: .migration/plans/T-0.15.replan3-review-4.md
BLOCKING: 0
SUMMARY: The DR-0015 limit increase eliminated the four-branch split. The collapsed single-branch plan perfectly incorporates the lost device preconditions, hard device cleanup paths, explicit CLI variable ingestions, and the end-to-end self-verification test. The test matrix recounts correctly to 14 total named tests, fulfilling all behavioral coverage requirements. I executed the required falsification bounds confirming the plan's assertions on B2 (empty env stdout), B4 (vacuous execute bypasses), and B5 (crashing missing flags).
NEXT: Implementer proceeds to execute the single-branch T-0.15a plan.
DECISIONS: none
