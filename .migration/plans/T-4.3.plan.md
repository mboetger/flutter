# Plan: T-4.3 — Flip the Public Default; Flag Remains as Opt-Out

## Proposed Changes

### 1. Switch Definitions (`engine/src/flutter/shell/common/switch_defs.h`)
- Update `kAndroidEmbedderApi` and add `kNoAndroidEmbedderApi`:
  ```cpp
  DEF_SWITCH(kAndroidEmbedderApi,
             "android-embedder-api",
             "Whether to enable the Android Embedder API adherence path. "
             "Defaults to true; pass --no-android-embedder-api to opt out.")
  DEF_SWITCH(kNoAndroidEmbedderApi,
             "no-android-embedder-api",
             "Whether to disable the Android Embedder API adherence path and "
             "opt out to the legacy implementation.")
  ```

### 2. Switches Parser (`engine/src/flutter/shell/common/switches.cc`)
- Update switch parsing to reference `FlagForSwitch(Switch::kNoAndroidEmbedderApi)`:
  ```cpp
  std::string android_embedder_api_value;
  if (command_line.GetOptionValue(FlagForSwitch(Switch::kAndroidEmbedderApi),
                                  &android_embedder_api_value)) {
    settings.android_embedder_api = android_embedder_api_value.empty() ||
                                    "true" == android_embedder_api_value;
  } else if (command_line.HasOption(
                 FlagForSwitch(Switch::kNoAndroidEmbedderApi))) {
    settings.android_embedder_api = false;
  }
  ```

### 3. Switches Unit Tests (`engine/src/flutter/shell/common/switches_unittests.cc`)
- Ensure tests verify default `true`, `--android-embedder-api=false`, and `--no-android-embedder-api`.

### 4. Ledger Update (`docs/platforms/android/embedder-api-migration/MIGRATION_LEDGER.md`)
- Update T-4.3 status in Stage 4 task table.

## Verification Strategy
- Clang format modified files.
- Run ratchet check (`python3 tools/android_embedder_deps.py --check`).
- Compile and test:
  `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native`
- Verify gates:
  `dart dev/tools/bin/migration_verify.dart --task T-4.3 --gates analysis,ratchet,native`
- Verify stack:
  `dart dev/tools/bin/migration_verify.dart --audit-stack`
