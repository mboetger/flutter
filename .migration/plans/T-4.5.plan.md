# Plan: T-4.5 — Delete the Flag and All Legacy Paths

## Proposed Changes

### 1. Switches and Settings Removal
- In `engine/src/flutter/shell/common/switch_defs.h`:
  - Remove `DEF_SWITCH(kAndroidEmbedderApi, ...)` and `DEF_SWITCH(kNoAndroidEmbedderApi, ...)`.
- In `engine/src/flutter/shell/common/switches.cc`:
  - Remove parsing of `Switch::kAndroidEmbedderApi` and `Switch::kNoAndroidEmbedderApi`.
- In `engine/src/flutter/common/settings.h`:
  - Remove `bool android_embedder_api` member.
- In `engine/src/flutter/shell/common/switches_unittests.cc`:
  - Remove `TEST(SwitchesTest, AndroidEmbedderApiFlag)`.

### 2. AndroidShellHolder Legacy Path Removal
- In `engine/src/flutter/shell/platform/android/android_shell_holder.h`:
  - Remove `bool IsAndroidEmbedderApiEnabled() const;`.
- In `engine/src/flutter/shell/platform/android/android_shell_holder.cc`:
  - Constructor: Directly call `InitializeEngine()`. Remove legacy shell initialization branch.
  - `Spawn()`: Directly call `SpawnEngine(...)`. Remove legacy spawn branch.
  - `Launch()`: Directly call `RunEngine(...)`. Remove legacy launch branch.
  - Remove `IsAndroidEmbedderApiEnabled()`.

### 3. PlatformViewAndroid Legacy Path Removal
- In `engine/src/flutter/shell/platform/android/platform_view_android.h`:
  - Remove `bool IsAndroidEmbedderApiEnabled() const;`.
  - Remove `const bool android_embedder_api_;` member variable.
- In `engine/src/flutter/shell/platform/android/platform_view_android.cc`:
  - Constructor: Remove initializer for `android_embedder_api_`.
  - Seams: Remove `if (android_embedder_api_)` branches and their corresponding legacy fallback code across:
    - `DispatchPointerDataPacket`
    - `SetViewportMetrics`
    - `DispatchSemanticsAction`
    - `SetSemanticsEnabled`
    - `SetAccessibilityFeatures`
    - `DispatchEmptyPlatformMessage`
    - `DispatchPlatformMessage`
    - `RegisterExternalTexture`
    - `RegisterImageTexture`
    - `OnDisplayPlatformView`
    - `OnHidePlatformView2`
    - `BeginFrameHC`
    - `EndFrameHC`
    - `CreateOverlaySurfaceHC`
    - `DestroyOverlaySurfacesHC`

### 4. Unit Tests Cleanup
- In `engine/src/flutter/shell/platform/android/android_shell_holder_unittests.cc`:
  - Remove legacy path tests (`settings.android_embedder_api = false`).
- In `engine/src/flutter/shell/platform/android/platform_view_android_unittests.cc`:
  - Remove `AndroidEmbedderApiFlagState`.
  - Remove legacy path tests (`*LegacyPath`).

### 5. Ledger Update (`docs/platforms/android/embedder-api-migration/MIGRATION_LEDGER.md`)
- Mark T-4.5 gating conditions as satisfied.
- Record completion of T-4.5 in Stage 4 task table.

## Verification Strategy
- Clang-format all touched C++ files.
- Verify ratchet: `python3 engine/src/flutter/tools/android_embedder_deps.py --check`.
- Run verification tool:
  `bin/cache/dart-sdk/bin/dart dev/tools/bin/migration_verify.dart --task T-4.5 --gates analysis,ratchet,native`
