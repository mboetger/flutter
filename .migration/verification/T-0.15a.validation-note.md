# T-0.15a — Validator note

- **Role:** validator (§D.4.5). Independent witness.
- **Branch:** `android-embedder-v8/t-0.15a-tooling-core`
- **Date:** 2026-09-15
- **Verdict: PASS**

---

## 1. Hygiene
- Tree clean at commit.
- Files modified scoped strictly to `dev/tools/` and `.migration/`.
- No golden updates (`--update-goldens` was not passed).
- No temporary or scratch files in working directory.

---

## 2. Gate Results

### Gate 1 — Format — PASS
```
bin/cache/dart-sdk/bin/dart format --output=none --set-exit-if-changed     dev/tools/bin/migration_verify.dart dev/tools/lib/migration_verify.dart     dev/tools/test/migration_verify_test.dart
Formatted 3 files (0 changed)
EXIT_CODE=0
```

### Gate 2 — Analyze — PASS
```
bin/cache/dart-sdk/bin/dart analyze --fatal-infos     dev/tools/bin/migration_verify.dart dev/tools/lib/migration_verify.dart     dev/tools/test/migration_verify_test.dart
No issues found!
EXIT_CODE=0
```

### Gate 3a — `dart test` — PASS (19/19)
```
bin/cache/dart-sdk/bin/dart test dev/tools/test/migration_verify_test.dart
All 19 tests passed!
EXIT_CODE=0
```

### Gate 3b — `bin/flutter test` — PASS (19/19)
```
bin/flutter test dev/tools/test/migration_verify_test.dart
All 19 tests passed!
EXIT_CODE=0
```

### Gate 4 — §A.3b composition matrix — NOT RUN
`"ran": false`. Reason: Tooling-only change. Touches no engine, compositor, texture, or rendering code.

### Gate 5 — Goldens — NOT APPLICABLE
No golden files read or written.

---

## 3. Dogfooding the Tool — PASS

1. **Gate Catalog Execution**:
   `dart dev/tools/bin/migration_verify.dart --task T-0.15a --gates analysis` successfully executes the mapped analysis command and outputs a valid JSON artifact.
2. **Tamper Detection**:
   Tampered `commit_sha` in artifact is strictly detected and rejected with exit code 1.
3. **Dirty Tree Guard**:
   Dirty working tree is rejected with error unless `--allow-dirty` is explicitly passed.
4. **License & Formatting**:
   License headers present on all files in `dev/tools/`.
