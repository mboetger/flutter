# DR-0057 — Flip Public Default of Android Embedder API with Opt-Out

- **Task:** T-4.3
- **Branch:** android-embedder-v8/t-4.3-public-default-flip
- **Author role:** planner
- **Date:** 2026-09-16
- **Status:** accepted
- **Confidence:** firm

## Context
Per `MIGRATION_LEDGER.md` §Stage 4 — Flip & Emancipation:
Task T-4.2 completed the 3-run soak verification across all 21 composition test mains and Devicelab targets with zero failures and performance within baseline noise bands.
Task T-4.3 addresses:
```
Flip the public default; flag remains as opt-out
Prototype gate: T-4.2 clean
Production gate: T-4.2 clean; release owner sign-off
```
With the tree default active in T-4.1 and soaked in T-4.2, the public switch definitions and help documentation must be updated to establish the Android Embedder API as the official public default, while formalizing the `--no-android-embedder-api` switch as the documented opt-out path for developers during the transition.

## Decision
1. Update `engine/src/flutter/shell/common/switch_defs.h`:
   - Update `kAndroidEmbedderApi` documentation: "Whether to enable the Android Embedder API adherence path. Defaults to true; pass --no-android-embedder-api to opt out."
   - Define `kNoAndroidEmbedderApi` ("no-android-embedder-api"): "Whether to disable the Android Embedder API adherence path and opt out to the legacy implementation."
2. In `engine/src/flutter/shell/common/switches.cc`:
   - Replace literal `"no-android-embedder-api"` with `FlagForSwitch(Switch::kNoAndroidEmbedderApi)`.
3. Add unit test verification in `switches_unittests.cc` validating that `kNoAndroidEmbedderApi` is recognized and disables the embedder API path.
4. Update `MIGRATION_LEDGER.md` recording completion of T-4.3.
5. Maintain dependency ratchet (deps <= 63, includes <= 121, nogncheck == 0).

## Consequences
- The Android Embedder API is now officially the public default path.
- The `--no-android-embedder-api` opt-out switch is formally defined in `switch_defs.h` and documented for users.
- Prototype gate is satisfied (T-4.2 clean).
