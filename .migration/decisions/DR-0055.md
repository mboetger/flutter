# DR-0055 — Default --android-embedder-api to On in the Tree

- **Task:** T-4.1
- **Branch:** android-embedder-v8/t-4.1-default-flag-on
- **Author role:** planner
- **Date:** 2026-09-16
- **Status:** accepted
- **Confidence:** firm

## Context
Per `MIGRATION_LEDGER.md` §Stage 4 — Flip & Emancipation:
All Stage 3 tasks (T-3.0 through T-3.8 and T-3.T1 through T-3.T6) are complete, verified, and pushed to `origin`.
Every composition mode in §B.1 has converged behind `--android-embedder-api` (`embedder-behind-flag`).
The Stage 3 exit criteria are fully met.
Task T-4.1 addresses:
```
Default --android-embedder-api to on in the tree
```
This is the first task of Stage 4 (Flip & Emancipation).
The default setting for `android_embedder_api` in `Settings` and `switches.cc` must be flipped to `true`.
The emergency opt-out `--no-android-embedder-api` and `--android-embedder-api=false` remains 100% intact and functional, routing to the legacy path when explicitly requested.

Requirements:
1. Default `settings.android_embedder_api` to `true` in `engine/src/flutter/common/settings.h`.
2. Default `settings.android_embedder_api` to `true` in `engine/src/flutter/shell/common/switches.cc` when the switch is omitted.
3. Update switch unit tests in `engine/src/flutter/shell/common/switches_unittests.cc` to reflect the new default while verifying that `--no-android-embedder-api` and `--android-embedder-api=false` correctly disable the flag.
4. Update platform view unit tests in `engine/src/flutter/shell/platform/android/platform_view_android_unittests.cc` to verify the default is now enabled and that explicit `--android-embedder-api=false` opts out to the legacy path.
5. In `MIGRATION_LEDGER.md`:
   - Check off the Stage 3 exit gate items.
   - Advance all six composition modes in §B.1 to `embedder-default` with last verified task `T-4.1`.
6. Maintain dependency ratchet (deps <= 63, includes <= 121, nogncheck == 0).

## Decision
1. In `engine/src/flutter/common/settings.h:268`:
   Change:
   ```cpp
   bool android_embedder_api = true;
   ```
2. In `engine/src/flutter/shell/common/switches.cc:477`:
   Change:
   ```cpp
   settings.android_embedder_api = true;
   ```
3. In `engine/src/flutter/shell/common/switches_unittests.cc`:
   Update `TEST(SwitchesTest, AndroidEmbedderApiFlag)`:
   - Verify default without args sets `settings.android_embedder_api == true`.
   - Verify explicit `--no-android-embedder-api` and `--android-embedder-api=false` set `settings.android_embedder_api == false`.
4. In `engine/src/flutter/shell/platform/android/platform_view_android_unittests.cc`:
   Update `TEST_F(PlatformViewAndroidTest, AndroidEmbedderApiFlagState)`:
   - Verify default `holder->IsAndroidEmbedderApiEnabled()` is `true`.
   - Verify explicit `settings.android_embedder_api = false` sets `holder->IsAndroidEmbedderApiEnabled()` to `false`.
5. In `MIGRATION_LEDGER.md`:
   - Mark Stage 3 exit criteria complete.
   - Advance §B.1 convergence tracker for all six modes to `embedder-default` verified by `T-4.1`.

## Consequences
- The Android Embedder API is now the default execution path across the Flutter engine tree.
- The legacy execution path remains available via `--no-android-embedder-api` or `--android-embedder-api=false`.
- All six composition modes in §B.1 are now in `embedder-default` status.
- Dependency ratchet remains <= baseline (63 deps, 121 includes, 0 nogncheck).
