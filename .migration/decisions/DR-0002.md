# DR-0002: Package placement for migration tool

**Date:** 2026-09-15
**Task:** T-0.15

## Context
We must confirm that placing the custom tooling inside `dev/tools/bin/migration_verify.dart` correctly works within the main Flutter repository structure without dependency resolution errors, considering `pubspec.yaml` using workspace resolution.

## Decision
The tool will reside in `dev/tools/bin/migration_verify.dart` and unit tests in `dev/tools/test/migration_verify_test.dart`. We verified that `dart dev/tools/bin/check_engine_version.dart --help` correctly parses arguments and builds the dependencies globally using workspace contexts. It runs cleanly from the repo root.
