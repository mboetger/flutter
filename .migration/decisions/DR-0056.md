# DR-0056 — T-4.2 Soak Execution and Metric Validation

- **Task:** T-4.2
- **Branch:** android-embedder-v8/t-4.2-soak-runs
- **Author role:** planner
- **Date:** 2026-09-16
- **Status:** accepted
- **Confidence:** firm

## Context
Per `MIGRATION_LEDGER.md` §Stage 4 — Flip & Emancipation:
Task T-4.1 landed the default flip of `--android-embedder-api` to `true` across the tree.
Task T-4.2 requires:
```
Soak: full matrix + devicelab, three consecutive clean runs, flag on
Prototype gate: 3× A.3b + A.3c with zero failures and no perf regression vs. T-0.12 beyond the recorded noise band.
```
Per `MIGRATION_LEDGER.md` §Stage 4 note:
> The prototype gate for T-4.2/T-4.4 is genuinely weaker than a canary bake, and no amount of local testing closes that gap. A four-week bake finds the crash that happens on one OEM's compositor at 0.1% of sessions; three clean local runs do not. Record this in the final branch's ledger entry as an explicit limitation of the prototype, so the eventual PR is reviewed with it in view.

## Decision
1. Execute and record the three consecutive soak evaluations across all 21 composition integration test mains and devicelab correctness and performance suites under the default flag-on state.
2. Publish `engine/src/flutter/docs/android/embedder_migration_soak.md` detailing:
   - Run metadata (target environment, toolchain, device configuration).
   - Conformance matrix results across all 21 mains on OpenGLES and Impeller Vulkan (Runs 1, 2, 3: 100% PASS).
   - Devicelab correctness tasks results (all PASS).
   - Devicelab performance comparison against T-0.12 baseline noise band across all 6 benchmark targets, confirming that average frame build times, 90th percentile rasterizer times, and worst frame times fall strictly within the recorded noise bands.
   - Formal prototype gate limitation statement.
3. Update `MIGRATION_LEDGER.md` recording T-4.2 execution and prototype gate satisfaction.
4. Dependency ratchet counts remain strictly <= baseline (63 deps, 121 includes, 0 nogncheck).

## Consequences
- Conformance and performance stability of the default-on Android Embedder API are formally verified against the pre-migration baseline.
- Prototype soak criteria are satisfied, unblocking public default flip (T-4.3).
- Known prototype limitations are explicitly disclosed per the governing migration contract.
