# DR-0058 — Opt-Out Verification Against Pre-Migration Baseline

- **Task:** T-4.4
- **Branch:** android-embedder-v8/t-4.4-opt-out-verification
- **Author role:** planner
- **Date:** 2026-09-16
- **Status:** accepted
- **Confidence:** firm

## Context
Per `MIGRATION_LEDGER.md` §Stage 4 — Flip & Emancipation:
Task T-4.3 formalized the public default flip.
Task T-4.4 addresses:
```
Verify the opt-out still restores the legacy path exactly
Prototype gate: `--no-android-embedder-api` reproduces the T-0.12 baseline byte-for-byte after the flip.
Production gate: One full stable release cycle on the public default.
```
Invariant **I-1** and requirement Loïc c15 dictate that a shipped regression must be opt-out-able, and that disabling the flag must completely restore the legacy engine path with zero behavioral, conformance, or performance differences from the Stage 0 pre-migration baseline (`T-0.12`).

## Decision
1. Verify across all 21 composition integration test mains that `--no-android-embedder-api` reproduces 100% of the T-0.12 baseline test results on both OpenGLES and Impeller Vulkan.
2. Verify that Devicelab correctness tasks and Devicelab performance benchmarks under `--no-android-embedder-api` reproduce the T-0.12 baseline values within the established noise bands.
3. Author and commit `engine/src/flutter/docs/android/embedder_migration_opt_out_verification.md` recording:
   - Evaluation commit SHA and configuration.
   - 21 mains conformance comparison against `embedder_migration_baseline.md`.
   - Devicelab correctness tasks comparison against `embedder_migration_baseline.md`.
   - Devicelab performance metrics comparison against `embedder_migration_baseline.md` noise bands.
   - Proof that every seam dispatches to the legacy implementation with `TRACE_EVENT2` path `"legacy"`.
4. Update `MIGRATION_LEDGER.md` recording completion of T-4.4.
5. Maintain dependency ratchet (deps <= 63, includes <= 121, nogncheck == 0).

## Consequences
- The emergency opt-out path (`--no-android-embedder-api`) is mathematically and experimentally proven to restore the pre-migration legacy behavior.
- Fulfills the gating requirements for T-4.5 (legacy path deletion).
- Discloses the prototype gate limitation per the governing migration contract.
