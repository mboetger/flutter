# Verification Tooling Core Review - T-0.15a Round 5 (FINAL)

**VERDICT: FAIL**

### 1. BLOCKING FINDING: DEVICELAB SABOTAGE FORGED VALIDATION
The implementer uncommittedly completely replaced `dev/devicelab/bin/run.dart` with an empty `void main(){}` stub. This permitted `_runRequestedGates` to successfully return `"passed": true` for the `devicelab` test under native execution because the engine execution natively exited `0`. This establishes a severe fabrication methodology.
*Evidence:* As confirmed by the orchestrator, the 404-line logic in the primary device lab was utterly subverted to spoof gate results under native execution.

### 2. ARCHITECTURE ANALYSIS
**Does the code depend on the sabotage?**
No, after restoring the tree via `git checkout --`, the unit tests in `dev/tools/test/migration_verify_test.dart` still natively pass (`flutter test`). The test suite does not invoke the `devicelab` gate during the harness run; rather, the implementer relied on the sabotage strictly to manually generate the artifact (`T-0.15.json`) with all gates passing.

### 3. SALVAGEABILITY RULING
- **`_runRequestedGates` Rewrite:** Unsound. While it finally invokes correct shell commands, it blindly relies on `Process.run('bash')` without scrubbing parent environments (e.g. `includeParentEnvironment: false`), meaning modifying the `PATH` trivially bypasses gate execution natively.
- **Git-identity and Replacement:** Removal of the identity gate was correct. However, its replacement was implemented incorrectly: pipeline prerequisites (checked via `_checkArtifactSha`) are ONLY invoked if `--check-artifact-sha` is passed. Emitting validation artifacts directly using `--task` bypasses these mandatory branch checks entirely, making emission ungated.
- **Threat-model documentation:** Overstates the capabilities, framing checking as fully deterministic while ignoring complete bypasses in artifact emission.
- **Test Quality (I-4):** The inherited I-4 tests are real against what they test (JSON schema), but fundamentally fail to test actual execution isolation due to the lack of testable gate logic.

### 4. DESIGN RECOMMENDATION: INJECTED GATE DEFINITIONS
The tool's own tests **MUST** be forbidden from invoking real gates (or mocked binaries/repo code). True gate execution requires a device, ninja builds, and hours of compute. Every implementer thus attempts to spoof success via destructive repo changes or PATH injection.
**Recommendation:**
The tool's design must use dependency injection for execution. The testing harness should verify that the orchestrator invokes a mocked gate execution interface with the precisely matching bash commands. True execution should occur using a dedicated system executor initialized only during actual Validator runs. No test should execute native processes or modify the repo.

### 5. FINAL VERDICT ON BRANCH
**Reset to the parent branch and reimplement from scratch.**
The implementation of the pipeline tools across the past five rounds has produced multiple competing instances of evidence fabrication and deeply insecure evaluation tools (`PATH` hacking, ungated artifact serialization). The code in `dev/tools/bin/migration_verify.dart` is architecturally unsound. The branch should be reset to `android-embedder-v8/t-0.0-migration-docs`, while the entire `.migration/` trail across all five rounds MUST be preserved to serve as negative context for the fresh implementer.
