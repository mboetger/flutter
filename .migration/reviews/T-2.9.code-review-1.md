# T-2.9 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (All 11 `ExternalViewEmbedder` virtual calls forward directly to underlying embedder without modification)
- [x] I-2 Assertions not weakened (Null checks, assertions, and preconditions preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify both `FlutterCompositor` struct shape and 1:1 transparent forwarding of all embedder lifecycle calls)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added comprehensive unit tests covering compositor struct shape, dynamic thread merging propagation, and transparent method delegation)
- [x] I-7 Tiered diff cap respected (Production diff ~150 lines, total diff ~500 lines including tests, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Changes strictly confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses existing platform-agnostic `embedder.h` structures)
- [x] I-11 All composition modes preserved (Zero files under `external_view_embedder/` deleted; all modes continue dispatching to existing embedders)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify `FlutterCompositor` struct shape and callbacks.**
   - Verified that `AndroidCompositorAdapter::GetFlutterCompositor()` returns a valid pointer.
   - Validated `struct_size == sizeof(FlutterCompositor)`.
   - Validated `user_data == adapter.get()`.
   - Validated `create_backing_store_callback`, `collect_backing_store_callback`, `present_view_callback`, `post_preroll_callback`, `begin_frame_callback`, `end_frame_callback` are populated.
   - Validated dynamic thread merging flag dynamically reflects `view_embedder_->SupportsDynamicThreadMerging()`.
   - Result: Passed (verified via `AndroidCompositorAdapterTest.FlutterCompositorStructShape` and `SupportsDynamicThreadMergingReflectsEmbedder`).

2. **Falsification Attempt 2: Verify transparent 1:1 forwarding to underlying embedder.**
   - Verified all 11 `ExternalViewEmbedder` methods forward cleanly:
     - `PrerollCompositeEmbeddedView`, `CompositeEmbeddedView`, `SubmitFlutterView`, `PostPrerollAction`, `GetRootCanvas`, `BeginFrame`, `PrepareFlutterView`, `CancelFrame`, `EndFrame`, `SupportsDynamicThreadMerging`, `Teardown`.
   - Verified auxiliary methods (`CollectView`, `PushVisitedPlatformView`, filter and clip push methods) forward cleanly.
   - Verified `GetUsedThisFrame()` toggles properly on `BeginFrame` / `EndFrame`.
   - Result: Passed (verified via `AndroidCompositorAdapterTest.TransparentForwarding`).

3. **Falsification Attempt 3: Verify wiring into `PlatformViewAndroid`.**
   - Verified `PlatformViewAndroid::CreateExternalViewEmbedder()` instantiates `AndroidExternalViewEmbedderWrapper` wrapped inside `AndroidCompositorAdapter`.
   - Verified `PlatformViewAndroid::GetCompositorAdapterForTesting()` returns the adapter and its `FlutterCompositor`.
   - Result: Passed (verified via `PlatformViewAndroidTest.PlatformViewDelegateSurfaceAndContextFallback`).

4. **Falsification Attempt 4: Dependency ratchet and include neutrality.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-2.9
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-2.9.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0038
SUMMARY:   Successfully introduced AndroidCompositorAdapter in front of AndroidExternalViewEmbedderWrapper, exposing the FlutterCompositor C-ABI shape while forwarding all ExternalViewEmbedder virtual calls transparently. Verified all invariants, ratchet counts (121 <= 121 includes, 63 <= 63 deps), wiring, and code formatting.
NEXT:      validator
```
