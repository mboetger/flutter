# Verification Tooling Core Review - T-0.15a (Attempt 3, Round 2)

**VERDICT: REVISE**

### 1. ROUND 1 BLOCKING FINDINGS DISPOSAL
* **(1) D1 Tamper Artifact Passing**: `accepted-resolved`. The uppercase bug was appropriately dropped, and `_checkArtifactSha(rest)` now correctly targets the task structure dynamically. Unit testing reliably extracts the physical path logic successfully and reliably reports string mismatches via targeted testing bounds (e.g. `expect(tamperedErr.contains(...), true)`).
* **(2) D2 Silent Dirt Tolerance**: `accepted-resolved`. The `dirtStatus.paths.isNotEmpty` check structurally exports into `"dirty": true` properly natively, verified physically outside via `--task T-0.15a --allow-dirty`.
* **(3) D3 Missing Requirements Matrix**: `accepted-resolved`. The tests evaluating `Req 5` (`--audit-stack` logic verification) and `Req 7` (`--device-lock` protections) were safely restored into the core matrix perfectly checking internal integrity bounds cleanly.

### 2. D4 — UNHANDLED CRASHES & GATE CATALOGUE SCOPE (BLOCKING)
**What is wrong**: Attempting to test the implementation against physical reality natively (e.g., `dart dev/tools/bin/migration_verify.dart --task T-0.15a --gates analysis`) results in a loud `FormatException / ProcessException` trace (exit 255).
* **(a) Unhandled Crashes**: Regardless of context, crashing into stack traces unhandled violates I-1 ("no silent fallbacks/unhandled crashes"). An invalid `--gates` parameter must fail cleanly and strictly.
* **SCOPE RULING (b)**: The gate catalog **IS strictly in scope** for `T-0.15a`. The Approved Plan natively dictates: *"2. Explicitly map `--task` (orchestrating which task steps apply) and `--gates` (explicit selections: `native,robolectric,host,analysis,ratchet,matrix,devicelab`) parsing variables securely"*.
Because they are natively in scope, blindly forwarding the string value of `--gates` into `executor.run()` (which inadvertently treats "analysis" strictly as a physical binary path rather than mapping it to `dart analyze`) violates the plan logic. This perfectly reproduces the Validator's core empty-environment half-fixed issue: the system no longer dies on execution properties, but it fundamentally has no mapped internal binaries to natively run!
**Resolution**: Gracefully catch generic strings explicitly passed through `--gates`. Map the known catalog of commands correctly (`native`, `analysis`, etc) to their underlying literal execution strings, ensuring they handle edge executions intelligently rather than physically exploding natively.

### 3. OTHER OBSERVATIONS & EXPERIMENTS
- **End-to-End Hollow Loop**: The tool fails on end-to-end verifications of `--gates` directly because the test suite purely mocks `executor.run` outputs synchronously. Actively modifying `dev/devicelab/bin/run.dart` physically failed to break the isolated integration tests, confirming that the entire matrix currently acts vacuously. Once `D4` is properly natively mapped, this dynamic mapping will organically secure real dependencies.
- **Both Runners**: Both `dart test` and `bin/flutter test` sequentially executed identically cleanly with ZERO isolated deadlock conditions cleanly on the clean tree.
- **Symlinks & Ledger Constraints**: `DR-0014` natively treats `.migration/` symlinks as structurally disqualifying. I-7 evaluates locally correctly around 409 core productive lines (well under the Tiered 3000 cap), and DR-0016 GATES correctly cleanly persist within the central ledger schema securely.
