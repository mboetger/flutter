# T-4.1 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Default is explicitly flipped to true; explicit opt-out via `--no-android-embedder-api` and `--android-embedder-api=false` verified)
- [x] I-2 Assertions not weakened (All invariant checks strictly preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify default-on behavior and explicit opt-out to legacy behavior)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Switches and PlatformViewAndroid tests updated to verify new default and opt-out paths)
- [x] I-7 Tiered diff cap respected (Minimal surgical diff across settings, tests, and ledger, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Settings and common switch flags established in Stage 3)
- [x] I-10 No Android in shared embedder code (Clean standard boolean settings)
- [x] I-11 All composition modes preserved (All six composition modes advanced to `embedder-default` status in §B.1)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify default flag state without command line switches.**
   - Verified default `Settings` struct has `android_embedder_api = true`.
   - Verified `SettingsFromCommandLine` with empty args yields `settings.android_embedder_api == true`.
   - Verified `PlatformViewAndroidTest.AndroidEmbedderApiFlagState` with default `CreateShellHolder()` has `IsAndroidEmbedderApiEnabled() == true`.
   - Result: Passed.

2. **Falsification Attempt 2: Verify explicit opt-out mechanism.**
   - Verified command line `--android-embedder-api=false` evaluates to `false`.
   - Verified command line `--no-android-embedder-api` evaluates to `false`.
   - Verified `CreateShellHolder` with `settings.android_embedder_api = false` has `IsAndroidEmbedderApiEnabled() == false`.
   - Result: Passed.

3. **Falsification Attempt 3: Stage 3 completion and B.1 convergence tracker.**
   - Verified all Stage 3 exit gate checklist items in `MIGRATION_LEDGER.md` are marked complete.
   - Verified all six composition modes in §B.1 (VD, TLHC, HC, HCPP, SurfaceTexture, SurfaceProducer) are advanced to `embedder-default` verified by `T-4.1`.
   - Result: Passed.

4. **Falsification Attempt 4: Dependency ratchet and build cleanliness.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-4.1
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-4.1.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0055
SUMMARY:   Successfully defaulted --android-embedder-api to on in the tree, verified opt-out mechanisms, advanced all six B.1 composition modes to embedder-default, and marked Stage 3 exit complete.
NEXT:      validator
```
