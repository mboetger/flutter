# T-3.0 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Flag state is explicitly determined, logged, and traced)
- [x] I-2 Assertions not weakened (FML_CHECK and preconditions preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify default false, explicit true/false, --no-android-embedder-api, and embedder plumbing)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added tests in `switches_unittests.cc` and `platform_view_android_unittests.cc`)
- [x] I-7 Tiered diff cap respected (Production diff ~60 lines, total diff ~360 lines including tests and artifacts, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Core additions are standard engine settings plumbing; Android embedder changes strictly confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Standard engine switch naming and boolean settings field)
- [x] I-11 All composition modes preserved (Flag defaults to false, keeping all existing legacy paths active and intact)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify switch parsing across all variants.**
   - Verified default value is `false` when neither `--android-embedder-api` nor `--no-android-embedder-api` is passed.
   - Verified `--android-embedder-api` evaluates to `true`.
   - Verified `--android-embedder-api=true` evaluates to `true`.
   - Verified `--android-embedder-api=false` evaluates to `false`.
   - Verified `--no-android-embedder-api` evaluates to `false`.
   - Result: Passed (verified via `SwitchesTest.AndroidEmbedderApiFlag`).

2. **Falsification Attempt 2: Verify Android embedder caching and accessors.**
   - Verified `AndroidShellHolder::IsAndroidEmbedderApiEnabled()` correctly reflects `settings.android_embedder_api`.
   - Verified `PlatformViewAndroid::IsAndroidEmbedderApiEnabled()` correctly reflects `settings.android_embedder_api`.
   - Verified `TRACE_EVENT1("flutter", "AndroidEmbedderApiState", "enabled", ...)` emits state at initialization.
   - Result: Passed (verified via `PlatformViewAndroidTest.AndroidEmbedderApiFlagState`).

3. **Falsification Attempt 3: Dependency ratchet and build cleanliness.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-3.0
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-3.0.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0040
SUMMARY:   Successfully introduced --android-embedder-api runtime feature flag switch, parsed it into flutter::Settings, plumbed and cached it in AndroidShellHolder and PlatformViewAndroid with trace logging, and expanded the Stage 3 schedule in MIGRATION_LEDGER.md. All invariants, ratchet counts (121 <= 121 includes, 63 <= 63 deps), wiring, and code formatting verified.
NEXT:      validator
```
