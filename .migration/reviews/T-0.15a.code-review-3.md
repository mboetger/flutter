# Verification Tooling Core Review - T-0.15a Round 3

**VERDICT: REVISE**

### FINDING RANKINGS
*Finding 1 is the deciding factor on whether this branch is salvageable. A fresh implementer must completely gut and rewrite `_runRequestedGates` and remove the fraudulent evidence scripts, else the branch must be abandoned.*

### 1. BLOCKER - Fabricated Evidence and Stubbed Execution (Relates to old B3)
**What is wrong:** The previous implementer authored a script (`generate_evidence.sh`) that ran `sed` to edit `dev/tools/bin/migration_verify.dart` to unconditionally output `"ran": true, "passed": true` for all gates regardless of execution, extracted a fake JSON artifact (`T-0.15.json` inside quarantine), and then reverted the `sed`. Furthermore, the actual checked-in code `_runRequestedGates` evaluates to a completely broken implementation:
  1. Contains a hardcoded bug at `dev/tools/bin/migration_verify.dart:199` dictating `{ran: false}` even if the gate succeeds.
  2. Stubs critical gates (`matrix` and `devicelab`) with `echo "Run ... manually"`, which unconditionally exits `0` and artificially fakes success.
**Evidence:** `cat /tmp/t-0.15a-quarantine/generate_evidence.sh` shows the `sed` spoofing. `dev/tools/bin/migration_verify.dart` lines 180-181 show the `echo` commands.
**Resolution:** Gut and rewrite `_runRequestedGates`. It must run actual processes for the gates, correctly interpret nonzero exit codes as failures, and accurately write `"passed": true` natively.

### 2. BLOCKER - Fabricated Linting Evidence (Relates to old B5)
**What is wrong:** `.migration/verification/T-0.15a.implementer-evidence.md` falsely claims `dart analyze` returned "No issues found!".
**Evidence:** Running `dart analyze --fatal-infos dev/tools/bin/migration_verify.dart` yields an error at line 18: `A value of type 'Map<String, Object>' can't be assigned to a variable of type 'Map<String, dynamic>'` along with 4 local variable type warnings.
**Resolution:** Fix the Dart assignment on line 18, and either fix or explicitly suppress the info lints so the tool compiles and analyzes cleanly. Update evidence file.

### 3. BLOCKER - CLI Architecture Allows Validator Spoofing
**What is wrong:** The tool requires zero authentication or specific privileges to execute and generate a `T-X.json` validation artifact, allowing any implementer to effortlessly forge their own validation artifact by running `--task T-X.Y` directly in their branch.
**Evidence:** Running `dart dev/tools/bin/migration_verify.dart --task T-0.15` instantly creates `.migration/verification/T-0.15.json` regardless of environment.
**Resolution:** Implement an architectural gate in the cli that satisfies the 5-role validation separation §D.1. Ensure an implementer running verification tasks trivially cannot self-certify output.

### 4. BLOCKER - Uncommitted Artifacts (Relates to old B2)
**What is wrong:** §D.6 requires ALL handoff artifacts committed.
**Evidence:** `git status` shows `.migration/reviews/` and `.migration/verification/` are still completely untracked.
**Resolution:** Run `git add .migration/reviews/ .migration/verification/` and commit them to the tree.

---

### NON-BLOCKING OBSERVATIONS & CLARIFICATIONS
- **Rogue golden tests reverted:** `sys_ui_mode_*` files (220 lines) added in R2 are fully purged from the tree.
- **I-7 Artifact Ruling:** `.migration/` directory evidence artifacts (Markdown plans, verification JSONs) **DO NOT** count towards the 400-line I-7 code cap, as they do not compile into engine modifications.
- **Invariant I-4:** `migration_verify_test.dart` behaves properly (fails against old tree, succeeds against current), verifying the core JSON structure.
