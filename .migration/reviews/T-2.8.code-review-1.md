# T-2.8 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (`Shell::Create` and all legacy execution paths remain intact and unmodified)
- [x] I-2 Assertions not weakened (All null checks and assertions on `task_runners_`, `shell_`, and `jni_facade_` retained)
- [x] I-3 Tests not weakened (All pre-existing unit tests and characterization tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify `FlutterProjectArgs` alignment, field populations, and callback delegations)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added 3 new unit tests covering `FlutterProjectArgs` construction, callback forwarding, and customization)
- [x] I-7 Tiered diff cap respected (Production diff ~120 lines, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Changes strictly confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses existing platform-agnostic `embedder.h` structures)
- [x] I-11 All composition modes preserved (Composition pipeline unaffected)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify `FlutterProjectArgs` construction and C-ABI shape.**
   - Verified that `AndroidShellHolder::GetProjectArgsForTesting()` returns a valid pointer.
   - Validated `struct_size == sizeof(FlutterProjectArgs)`.
   - Validated `custom_task_runners` references valid `FlutterCustomTaskRunners` from `AndroidTaskRunners`.
   - Validated `icu_data_path` and `assets_path` strings match `settings_`.
   - Validated `asset_resolvers` is null before launch, and properly populated with `FlutterAssetResolver` on launch.
   - Result: Passed (verified via `AndroidShellHolder.ProjectArgsConstruction`).

2. **Falsification Attempt 2: Verify callback trampolines dispatching to JNI.**
   - Verified `on_pre_engine_restart_callback(holder)` invokes `jni->FlutterViewOnPreEngineRestart()`.
   - Verified `set_application_locale_callback("fr-FR", holder)` invokes `jni->FlutterViewSetApplicationLocale("fr-FR")`.
   - Verified `get_scaled_font_size_callback(16.0, 2, holder)` invokes `jni->FlutterViewGetScaledFontSize(16.0, 2)`.
   - Verified `dart_deferred_library_request_callback(123, holder)` invokes `jni->RequestDartDeferredLibrary(123)`.
   - Result: Passed (verified via `AndroidShellHolder.ProjectArgsCallbacksForwardToJNI`).

3. **Falsification Attempt 3: Dependency ratchet and include neutrality.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 123).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-2.8
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-2.8.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0037
SUMMARY:   Successfully restructured AndroidShellHolder to construct and maintain the FlutterProjectArgs C-ABI shape. Verified all invariants, ratchet counts (121 <= 123 includes, 63 <= 63 deps), callback delegation, and code formatting.
NEXT:      validator
```
