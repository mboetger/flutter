# T-2.7 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Raster thread synchronization, latch waits, and lifecycle order preserved identically)
- [x] I-2 Assertions not weakened (All null checks, DCHECKs, and thread safety assertions retained)
- [x] I-3 Tests not weakened (All 7 pre-existing Stage 0 characterization tests in `platform_view_android_surface_lifecycle_unittests.cc` preserved unmodified)
- [x] I-4 Parity proven by test (Characterization tests and new unit tests verify identical surface attachment, teardown, thread affinity, and idempotency)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added 3 new unit tests covering `AndroidSurfaceLifecycle` availability tracking, GPU availability transitions, and direct delegate callbacks)
- [x] I-7 Tiered diff cap respected (Production diff ~200 lines, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Changes strictly confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses existing platform-agnostic `embedder.h` enum `FlutterGpuAvailability`)
- [x] I-11 All composition modes preserved (Surface lifecycle applies identically across all rendering backends)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify Stage 0 characterization tests and behavioral equivalence.**
   - Verified that all 7 Stage 0 characterization tests in `platform_view_android_surface_lifecycle_unittests.cc` (`NotifyCreatedValidSurface`, `NotifyDestroyedReleasesGpuResources`, `BackgroundAndForegroundCycle`, `NotifySurfaceWindowChangedMidFrame`, `NotifyDestroyedWithFrameInFlight`, `DoubleNotifyCreatedIdempotency`, `DoubleNotifyDestroyedIdempotency`) pass completely unmodified.
   - Thread synchronization on the raster runner preserves identical synchronous completion semantics.
   - Result: Passed.

2. **Falsification Attempt 2: Verify `AndroidSurfaceLifecycle` unit test suite and state tracking.**
   - Verified that `AndroidSurfaceLifecycle` properly tracks surface availability (`IsSurfaceAvailable()`) transitioning between `false` and `true` across `NotifyCreated` and `NotifyDestroyed`.
   - Verified `SetGpuAvailability` transitions (`kAvailable`, `kFlushAndMakeUnavailable`, `kUnavailable`).
   - Verified that `Delegate` callbacks (`OnSurfaceCreated`, `OnSurfaceDestroyed`, `OnScheduleFrame`, `OnInstallFirstFrameCallback`, `OnSetGpuAvailability`) are invoked with exact identical ordering.
   - Result: Passed (verified via `SurfaceLifecycleAvailabilityTracking`, `GpuAvailabilityTransitions`, and `DirectLifecycleDelegateCallbacks`).

3. **Falsification Attempt 3: Dependency ratchet and include neutrality.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 123).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-2.7
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-2.7.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0036
SUMMARY:   Successfully extracted surface lifecycle and raster thread synchronization from PlatformViewAndroid into AndroidSurfaceLifecycle, matching the lifecycle shape from T-0.7 / T-1.2. Verified all invariants, characterization tests unmodified, ratchet counts (121 <= 123 includes, 63 <= 63 deps), and code formatting.
NEXT:      validator
```
