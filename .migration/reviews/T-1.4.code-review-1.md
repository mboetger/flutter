# T-1.4 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (host embedder tests exercise Dart deferred library request callback, error dispatch, load dispatch, and proc table)
- [x] I-5 Test targets wired (`platform_view_embedder_unittests.cc` and `embedder_unittests.cc` are pre-wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (added unit tests in `platform_view_embedder_unittests.cc` and integration tests in `embedder_unittests.cc`)
- [x] I-7 Tiered diff cap respected (modular, scoped additions to embedder API and platform view)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (all changes contained within `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (zero Android headers, types, or symbols in `embedder.h`, `embedder.cc`, `platform_view_embedder.*`, `embedder_engine.*`)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1).**
   - Verified that `FlutterRequestDartDeferredLibraryCallback`, `FlutterLoadDeferredLibraryInfo`, and `FlutterLoadDeferredLibraryErrorInfo` are declared unconditionally.
   - Verified that `dart_deferred_library_request_callback` is appended to the end of `FlutterProjectArgs`.
   - Verified that versioned info struct pattern (§6.1 Rule 3) is followed for `FlutterEngineLoadDartDeferredLibrary` and `FlutterEngineLoadDartDeferredLibraryError`.
   - Verified that both functions are exported via `FLUTTER_EXPORT` and appended to `FlutterEngineProcTable` via `SET_PROC`.
   - Verified that `SAFE_ACCESS` is used consistently for accessing all struct members.
   - Verified that `FLUTTER_ENGINE_VERSION` is untouched.
   - Verified that zero Android types or headers are present in `shell/platform/embedder/`.
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify request and load dispatch pathways.**
   - Inspected `PlatformViewEmbedder::RequestDartDeferredLibrary(loading_unit_id)`. Verified that when `request_dart_deferred_library_callback` is provided in the dispatch table, it correctly forwards the loading unit ID; when null, it safely no-ops.
   - Inspected `EmbedderEngine::LoadDartDeferredLibrary` and `EmbedderEngine::LoadDartDeferredLibraryError`. Verified that if engine is invalid, they safely return `false`. When valid, they forward directly to `Shell::LoadDartDeferredLibrary` and `Shell::LoadDartDeferredLibraryError`.
   - Inspected `FlutterEngineLoadDartDeferredLibrary` and `FlutterEngineLoadDartDeferredLibraryError` in `embedder.cc`. Verified parameter validation: checks engine validity, info pointer, struct size, non-null snapshot data/instructions buffers, non-null error message, and wraps buffers in `fml::NonOwnedMapping`.
   - Result: Passed. Dispatch pathways and safety checks verified.

3. **Falsification Attempt 3: Verify host test coverage and proc table consistency.**
   - Inspected `platform_view_embedder_unittests.cc`: verified `DispatchesDartDeferredLibraryRequest` and `DartDeferredLibraryRequestWithoutCallback`.
   - Inspected `embedder_unittests.cc`: verified `DartDeferredLibraryCallbacks` covering invalid engine handle, null info pointer, invalid struct size, null snapshot data/instructions, null error message, proc table mapping, request callback dispatching via `PlatformView`, and successful load/error dispatching.
   - Result: Passed. Host test coverage is comprehensive.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.4
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.4.code-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   Dart deferred components C-ABI (FlutterRequestDartDeferredLibraryCallback, FlutterLoadDeferredLibraryInfo, FlutterLoadDeferredLibraryErrorInfo), FlutterEngineLoadDartDeferredLibrary, FlutterEngineLoadDartDeferredLibraryError, PlatformViewEmbedder dispatch, EmbedderEngine wiring, and comprehensive host tests pass all invariants and Stage 1 rules.
NEXT:      validator
```
