# T-3.1 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Flag state is explicitly branched; SendPointerEvents returns explicit FlutterEngineResult)
- [x] I-2 Assertions not weakened (Pointer event preconditions and bounds checks preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify legacy routing with flag false, embedder routing with flag true, and argument validation)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added 3 new unit tests in `platform_view_android_unittests.cc`: `PointerDispatchLegacyPath`, `PointerDispatchEmbedderApiPath`, `SendPointerEventsValidation`)
- [x] I-7 Tiered diff cap respected (Production diff ~150 lines, total diff ~530 lines including tests and artifacts, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses platform-agnostic `FlutterPointerEvent` from `embedder.h`)
- [x] I-11 All composition modes preserved (Pointer event dispatch works across all composition modes)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify legacy vs embedder path separation and tracing.**
   - Verified default/flag off emits `TRACE_EVENT1("flutter", "PlatformViewAndroid::DispatchPointerDataPacket", "path", "legacy")` and routes through `platform_view_->DispatchPointerDataPacket(std::move(packet))`.
   - Verified flag on emits `TRACE_EVENT1("flutter", "PlatformViewAndroid::DispatchPointerDataPacket", "path", "embedder_api")` and routes through `SendPointerEvents(...)` without dual-routing `platform_view_`.
   - Result: Passed (verified via `PlatformViewAndroidTest.PointerDispatchLegacyPath` and `PointerDispatchEmbedderApiPath`).

2. **Falsification Attempt 2: Verify SendPointerEvents parameter validation and field conversions.**
   - Verified null pointer events array returns `kInvalidArguments`.
   - Verified zero event count returns `kInvalidArguments`.
   - Verified valid pointer event conversions preserve phases, coordinates, devices, and kinds, returning `kSuccess`.
   - Result: Passed (verified via `PlatformViewAndroidTest.SendPointerEventsValidation`).

3. **Falsification Attempt 3: Dependency ratchet and build cleanliness.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-3.1
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-3.1.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0041
SUMMARY:   Successfully implemented SendPointerEvents C-ABI seam in PlatformViewAndroid and routed DispatchPointerDataPacket behind --android-embedder-api with TRACE_EVENT logging and zero dual-routing. Added unit tests for legacy path, embedder path, and parameter validation. Ratchet counts (63 deps, 121 includes, 0 nogncheck) and build pass cleanly.
NEXT:      validator
```
