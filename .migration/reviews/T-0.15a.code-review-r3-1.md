# Verification Tooling Core Review - T-0.15a (Attempt 3, Round 1)

**VERDICT: REVISE**

### STRUCTURAL DIAGNOSIS OF VALIDATOR FAILURES

**1. D1 — `--check-artifact-sha` TAMPER TEST BYPASS ALGORITHM (BLOCKING)**
**What is wrong**: The script attempts to parse branch names via regex `[tT]-[\d.a-zA-Z]+` and aggressively uppercases the extracted branch matching (e.g. `T-0.15A.json`), which completely drops case-sensitivity matching against natively generated files like `t-0.15a.json`. The unit test `Req 9: fails to validate tampered deadbeef hashes on disk` purely passes against this broken behavior because the implementer *manually hardcoded* an uppercase mocked file (`T-REQ9.json`) into the `MemoryFileSystem`, structurally bypassing the artifact generator internally. It tests an artificial uppercase sandbox, which is how it passes while being functionally broken against reality.
**Evidence**: `fs.file('${verificationDir.path}/T-REQ9.json').writeAsStringSync(...)` on line 211 of the test mimics the explicit failure of `taskId.toUpperCase()`.
**Resolution**: Strip the uppercase formatting strictly (`match.group(0)!`) and enforce test integration spanning dynamic generation-to-validation pipelines.

**2. D2 — SILENT DIRT FALLBACK (BLOCKING)**
**What is wrong**: `DR-0013` requires `.migration/` dirt to be tolerated and formally serialized as `"dirty": true`. The script natively tolerates the dirt inside `_isTreeHardDirty` but completely silently ignores serializing the state payload to the final `artifact` map, structurally dropping the requirement.
**Evidence**: Native execution `bin/cache/dart-sdk/bin/dart dev/tools/bin/migration_verify.dart --task t-0.15a --allow-dirty` containing dirty files inside `.migration/` natively resulted in schema output identical to clean states (`"ran": false`), completely devoid of `"dirty": true`.
**Resolution**: Actively mutate the final JSON dictionary mapping to append the `"dirty"` flag logically.

**3. D3 — MISSING REQUIREMENTS TESTS OMISSIONS (BLOCKING)**
**What is wrong**: The plan clearly dictates 14 Matrix tests, yet the unit test file explicitly contains 13.
**Missing tests**: `Req 5` (for `--audit-stack` logic chains) and `Req 7` (for `--device-lock` protections) are both fully absent.
**Evidence**: A review of `dev/tools/test/migration_verify_test.dart` reveals zero implementations calling `--device-lock` or `--audit-stack`. This critically leaves both execution mechanisms functionally executing totally blindly, inherently leading directly back to the B5 crash behavior (casting `_LocalFile` to `File`).
**Resolution**: Author the remaining `Req 5` and `Req 7` tests inside the mock boundaries explicitly as assigned by the plan.

### OTHER OBSERVATIONS
- **Env Propagation**: Tested `/usr/bin/env` safely passes inherited structures dynamically, resolving B2 natively.
- **I-4 Controls**: I manually broke the `isDirtyHard`, `.flag('update-goldens')`, and `isCheckArtifactSha` checks. `Req 4`, `Req 8`, and `Req 1` correctly failed respectively natively (`Expected: <1> Actual: <0>`), ensuring the test mechanisms are functionally sound endpoints.
- **Devicelab Stub**: `dev/devicelab/bin/run.dart` gutted externally perfectly failed to influence the internal E2E execution tests, comprehensively confirming robust `FakeSystemCommandExecutor` CI sandbox constraints.
- **Runners Mandatory Executions**: Both `dart test` and `bin/flutter test` sequentially executed identically (with no deadlocks encountered).
