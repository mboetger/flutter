# T-1.2 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (host embedder test exercises surface lifecycle and GPU availability)
- [x] I-5 Test targets wired (`embedder_unittests` runs `embedder_unittests.cc` and `embedder_unittests_proctable.cc`)
- [x] I-6 Net coverage preserved (added `SurfaceLifecycleAndGpuAvailability` test case)
- [x] I-7 Tiered diff cap respected (well within Tier 1 cap)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (only `shell/platform/embedder/` touched)
- [x] I-10 No Android in shared embedder code (zero Android headers or symbols in `embedder.h`, `embedder.cc`, `embedder_engine.*`)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1).**
   - Verified that `FlutterGpuAvailability` enum is properly declared with values matching `flutter::GpuAvailability`.
   - Verified that `FlutterEngineSetGpuAvailability`, `FlutterEngineNotifySurfaceCreated`, and `FlutterEngineNotifySurfaceDestroyed` are strictly exported via `FLUTTER_EXPORT`.
   - Verified that function pointers are strictly appended to `FlutterEngineProcTable` and initialized via `SET_PROC` in `FlutterEngineGetProcAddresses`.
   - Verified that `FLUTTER_ENGINE_VERSION` is untouched.
   - Verified that zero Android types or headers are present in `shell/platform/embedder/`.
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify thread affinity and synchronous dispatch lifecycle.**
   - Inspected `EmbedderEngine::NotifyCreated()`, `EmbedderEngine::NotifyDestroyed()`, and `EmbedderEngine::SetGpuAvailability()`.
   - Verified that when called from an off-platform thread (e.g. from platform or worker threads), all three methods synchronously dispatch to the platform task runner via `fml::AutoResetWaitableEvent latch` and block until completion.
   - Verified that when called directly on the platform task runner, they execute immediately without deadlocking or posting redundant tasks.
   - Verified that `NotifyDestroyed` teardown completes synchronously before returning, matching Android OS `surfaceDestroyed` contract.
   - Result: Passed. Synchronous platform-runner execution guarantees surface teardown safety.

3. **Falsification Attempt 3: Verify host test coverage and proc table consistency.**
   - Inspected `TEST_F(EmbedderTest, SurfaceLifecycleAndGpuAvailability)` in `embedder_unittests.cc`.
   - Verified that all three states of `FlutterGpuAvailability` (`kFlutterGpuAvailabilityFlushAndMakeUnavailable`, `kFlutterGpuAvailabilityUnavailable`, `kFlutterGpuAvailabilityAvailable`) return `kSuccess`.
   - Verified that `FlutterEngineNotifySurfaceDestroyed` and `FlutterEngineNotifySurfaceCreated` succeed for `kFlutterImplicitViewId`.
   - Verified that invalid view IDs return error codes.
   - Verified that `embedder_unittests_proctable.cc` verifies non-null function pointers and uniqueness for the newly appended entries.
   - Result: Passed. Host test verification complete.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.2
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.2.code-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   C-ABI exports for surface lifecycle and GPU availability along with synchronous platform-runner dispatch in EmbedderEngine pass all migration invariants and C-ABI constraints.
NEXT:      validator
```
