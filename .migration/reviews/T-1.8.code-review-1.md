# T-1.8 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (all 30 parameters from T-0.4 parity contract mapped 1:1 into FlutterWindowMetricsEvent and ViewportMetrics)
- [x] I-5 Test targets wired (`embedder_unittests.cc` wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (new validation, display features nullness, full parity round-trip, and legacy struct size tests added)
- [x] I-7 Tiered diff cap respected (compact additions to `embedder.h`, `embedder.cc`, and `embedder_unittests.cc`)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (all changes in `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (zero Android headers, types, or symbols in `embedder.h`, `embedder.cc`, or tests)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1) and struct extension.**
   - Verified that `display_features_count`, `display_features_bounds`, `display_features_type`, `display_features_state`, `physical_padding_*`, `physical_system_gesture_inset_*`, `physical_touch_slop`, and `physical_display_corner_radius_*` are appended to the end of `FlutterWindowMetricsEvent`.
   - Verified that no existing fields in `FlutterWindowMetricsEvent` were moved, resized, or reordered.
   - Verified that `FLUTTER_ENGINE_VERSION` is untouched (§G.5, §G.9 Item 2).
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify translation logic in `MakeViewportMetricsFromWindowMetrics`.**
   - Verified that `SAFE_ACCESS(flutter_metrics, member, fallback)` is used for all newly appended fields.
   - Verified that physical padding is validated to be non-negative and within window dimensions.
   - Verified that display features count overflow (`SIZE_MAX / 4`) is guarded against, and null buffers when `count > 0` are rejected.
   - Verified that `physical_touch_slop` and `physical_display_corner_radius_*` default to `-1.0` (unset sentinel).
   - Result: Passed.

3. **Falsification Attempt 3: Verify host test coverage.**
   - Inspected `tests/embedder_unittests.cc`:
     - `InvalidFlutterWindowMetricsEvent` validates rejection of negative padding and excessive padding, and acceptance of valid padding.
     - `WindowMetricsEventDisplayFeaturesValidation` validates rejection of null buffers when count > 0, and acceptance of valid display features.
     - `WindowMetricsEventAllParityFields` tests all 17 new fields with non-default values and asserts success.
     - `WindowMetricsEventWithLegacyStructSize` tests legacy struct sizes with safe default fallbacks.
   - Result: Passed. Host test coverage is comprehensive.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.8
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.8.code-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   Window metrics parity contract fields (physical padding, system gesture insets, touch slop, display features, corner radii) added to FlutterWindowMetricsEvent, translated and validated in MakeViewportMetricsFromWindowMetrics, and verified with unit tests.
NEXT:      validator
```
