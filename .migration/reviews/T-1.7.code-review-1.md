# T-1.7 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (unit and integration tests verify callback dispatch and no-op handling)
- [x] I-5 Test targets wired (`platform_view_embedder_unittests.cc` and `embedder_unittests.cc` wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (added unit tests in `platform_view_embedder_unittests.cc` and integration test in `embedder_unittests.cc`)
- [x] I-7 Tiered diff cap respected (compact additions to `embedder.h`, `embedder.cc`, `platform_view_embedder.h`, `platform_view_embedder.cc`, and tests)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (all changes within `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (zero Android headers, types, or symbols in `embedder.h`, `embedder.cc`, or tests)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1) and struct extension.**
   - Verified that `FlutterSetApplicationLocaleCallback` is defined as a C function pointer type.
   - Verified that `set_application_locale_callback` is appended to the end of `FlutterProjectArgs`.
   - Verified that no existing fields in `FlutterProjectArgs` were moved or modified.
   - Verified that `FLUTTER_ENGINE_VERSION` is untouched (§G.5, §G.9 Item 2).
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify translation logic in `embedder.cc` and `platform_view_embedder.cc`.**
   - Verified that `SAFE_ACCESS(args, set_application_locale_callback, nullptr)` safely handles older struct sizes.
   - Verified that `PlatformViewEmbedder::SetApplicationLocale` checks for null callback before invoking.
   - Verified that string data is safely passed as `const char*` with guarantee of validity during callback execution.
   - Result: Passed.

3. **Falsification Attempt 3: Verify host test coverage.**
   - Inspected `platform_view_embedder_unittests.cc`: verified `DispatchesSetApplicationLocale` and `SetApplicationLocaleWithoutCallback`.
   - Inspected `tests/embedder_unittests.cc`: verified `CanReceiveApplicationLocale` runs `set_application_locale_main` from Dart, calls `PlatformDispatcher.instance.setApplicationLocale`, and verifies callback invocation with `"pt-BR"`.
   - Result: Passed. Host test coverage is comprehensive.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.7
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.7.code-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   SetApplicationLocale callback added to FlutterProjectArgs, wired through PlatformViewEmbedder, and verified with unit and integration tests.
NEXT:      validator
```
