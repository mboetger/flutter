# T-2.10 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Texture operations return explicit `FlutterEngineResult` status codes; null delegate / invalid ID cleanly fails)
- [x] I-2 Assertions not weakened (FML_CHECK and preconditions preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify Embedder API C-ABI shape `RegisterExternalTexture`, `UnregisterExternalTexture`, `MarkExternalTextureFrameAvailable` and legacy JNI paths)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added comprehensive unit tests covering C-ABI operations, legacy SurfaceTexture/ImageTexture registration, and error paths)
- [x] I-7 Tiered diff cap respected (Production diff ~200 lines, total diff ~680 lines including tests and artifacts, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Changes strictly confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses platform-agnostic `embedder.h` structures / enums)
- [x] I-11 All composition modes preserved (Zero files under `external_view_embedder/` deleted; neither external texture implementation deleted; both paths keep working)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify Embedder C-ABI external texture seam.**
   - Verified that `RegisterExternalTexture(int64_t)` returns `kSuccess` when delegate succeeds, `kInvalidArguments` on invalid ID, and `kInternalInconsistency` on null delegate.
   - Verified that `UnregisterExternalTexture(int64_t)` returns `kSuccess` on success, `kInvalidArguments` on invalid ID, and `kInternalInconsistency` on null delegate.
   - Verified that `MarkExternalTextureFrameAvailable(int64_t)` returns `kSuccess` on success, `kInvalidArguments` on invalid ID, and `kInternalInconsistency` on null delegate.
   - Result: Passed (verified via `AndroidExternalTextureAdapterTest.RegisterUnregisterMarkEmbedderShape` and `NullDelegateReturnsError`).

2. **Falsification Attempt 2: Verify legacy JNI paths keep working.**
   - Verified that `RegisterSurfaceTexture` instantiates the correct external texture subclass (`GLSkia`, `GLImpeller`, or `VKImpeller`) based on `RenderingApi`.
   - Verified that `RegisterImageTexture` instantiates the correct image external texture subclass (`GLSkia`, `GLImpeller`, or `VKImpeller`) based on `RenderingApi`.
   - Verified that textures register cleanly with `Delegate::OnRegisterExternalTexture` and unregister cleanly via `UnregisterExternalTexture`.
   - Result: Passed (verified via `AndroidExternalTextureAdapterTest.RegisterSurfaceTextureLegacyPath` and `RegisterImageTextureLegacyPath`).

3. **Falsification Attempt 3: Verify wiring into `PlatformViewAndroid`.**
   - Verified `PlatformViewAndroid` implements `AndroidExternalTextureAdapter::Delegate`.
   - Verified `PlatformViewAndroid::RegisterExternalTexture`, `RegisterImageTexture`, `UnregisterTexture`, and `MarkTextureFrameAvailable` delegate to `external_texture_adapter_`.
   - Verified `GetExternalTextureAdapterForTesting()` returns the adapter.
   - Result: Passed (verified via `PlatformViewAndroidTest.ExternalTextureAdapterWiring`).

4. **Falsification Attempt 4: Dependency ratchet and include neutrality.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-2.10
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-2.10.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0039
SUMMARY:   Successfully introduced AndroidExternalTextureAdapter in front of SurfaceTextureExternalTexture and ImageExternalTexture, exposing the FlutterEngineRegisterExternalTexture C-ABI shape while preserving legacy JNI texture registration and dynamic backend selection. Verified all invariants, ratchet counts (121 <= 121 includes, 63 <= 63 deps), wiring, and code formatting.
NEXT:      validator
```
