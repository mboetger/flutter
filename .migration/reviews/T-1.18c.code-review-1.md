# T-1.18c Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (`DynamicThreadMergingNullCallbackError` explicitly tests that enabling dynamic thread merging without `post_preroll_callback` fails `FlutterEngineInitialize` with `kInvalidArguments`)
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (`DynamicThreadMergingLifecycle` verifies dynamic thread merging lifecycle through public C-APIs, including `MergeWithLease`, `ExtendLeaseTo`, `IsMerged`, and `IsOnPlatformThread` across `kFlutterPostPrerollResultSkipAndRetryFrame`, `kFlutterPostPrerollResultResubmitFrame`, and `kFlutterPostPrerollResultSuccess`)
- [x] I-5 Test targets wired (`embedder_unittests` compiles and executes new tests)
- [x] I-6 Net coverage preserved (new tests added, full suite green)
- [x] I-7 Tiered diff cap respected (180 lines added in `embedder_unittests.cc`, well within <= 1000 lines limit)
- [x] I-8 No nogncheck without bug (zero new nogncheck)
- [x] I-9 No shared-code changes outside embedder layer (only `embedder_unittests.cc` modified)
- [x] I-10 No Android in shared embedder code (pure generic embedder tests running on host)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

---

## Falsification Attempts
1. **Falsification Attempt 1: Test Invariant I-1 enforcement with null post_preroll_callback.**
   - Verified that `DynamicThreadMergingNullCallbackError` configures `supports_dynamic_thread_merging = true` and `post_preroll_callback = nullptr`.
   - Verified that `builder.InitializeEngine()` fails and returns an invalid engine.
   - Result: Passed. Verified that missing callback cannot bypass validation.

2. **Falsification Attempt 2: Verify null safety and proc table population for dynamic thread merger C-APIs.**
   - Verified `DynamicThreadMergingNullMergerSafe` exercises all 4 C-API functions (`FlutterRasterThreadMergerIsMerged`, `FlutterRasterThreadMergerIsOnPlatformThread`, `FlutterRasterThreadMergerMergeWithLease`, `FlutterRasterThreadMergerExtendLeaseTo`) when passed a null merger pointer, confirming they are safe no-ops and do not crash or corrupt memory.
   - Verified `DynamicThreadMergingProcTable` exercises `FlutterEngineGetProcAddresses` and checks that all 4 proc table pointers are populated.
   - Result: Passed.

3. **Falsification Attempt 3: Verify dynamic thread merging state machine and frame lifecycle execution.**
   - Verified `DynamicThreadMergingLifecycle` drives the full sequence on a dedicated `fml::Thread`:
     - Frame 1: Initial unmerged state observed (`FlutterRasterThreadMergerIsMerged` is false). Calls `FlutterRasterThreadMergerMergeWithLease(merger, 2)` and returns `kFlutterPostPrerollResultSkipAndRetryFrame`.
     - Frame 2 (retry): Merged state observed (`FlutterRasterThreadMergerIsMerged` and `FlutterRasterThreadMergerIsOnPlatformThread` both true). Calls `FlutterRasterThreadMergerExtendLeaseTo(merger, 5)` and returns `kFlutterPostPrerollResultResubmitFrame`.
     - Frame 3 (resubmit): Merged state preserved. Returns `kFlutterPostPrerollResultSuccess`.
     - Presentation callback invoked and signals `present_latch`.
     - `end_frame_callback` invoked on each frame and signals `end_frame_latch`.
     - Engine cleanly shut down on the thread task runner.
   - Result: Passed. All assertions satisfied in 265 ms.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.18c
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.18c.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0027
SUMMARY:   Dynamic thread merging host unit tests added to embedder_unittests.cc covering Invariant I-1 validation, proc table accessors, null safety, and full frame lifecycle thread merging.
NEXT:      validator
```
