# T-0.11 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test
- [x] I-5 Test targets wired
- [x] I-6 Net coverage preserved
- [x] I-7 Tiered diff cap respected (~250 lines)
- [x] I-8 No nogncheck without bug
- [x] I-9 No shared-code changes
- [x] I-10 No Android in shared embedder code
- [x] I-11 All composition modes preserved (primary defense mechanism)
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify all 7 Definition of Done criteria in T-0.11 ledger block are met.**
   - Explicit mode registry without path globbing: `CompositionMode` enum defined (`virtualDisplay`, `textureLayerHybrid`, `hybrid`, `hcpp`, `surfaceTexture`, `surfaceProducer`) with explicit `kCompositionModeMains` and `kGeneralIntegrationMains` mappings.
   - Empty-mode assertion: `executedPerMode` tracks executions; if any supported mode has 0 executions, `foundError` triggers with an explicit message naming the mode and citing Invariant I-11.
   - HCPP Vulkan-only constraint declared via self-documenting `checkModeSupport()`: Declares `ModeSupport.unsupported('HCPP structurally requires Impeller Vulkan and Android API 34; on OpenGLES apps fall back to HC/TLHC')` for OpenGLES.
   - GLES HCPP fallback coverage: Explicit test added to run `lib/hcpp/upgrade_legacy_pv_types_main.dart` with `useHCPPFlag: true` and `EXPECT_HCPP=false` on OpenGLES to assert graceful fallback to HC/TLHC.
   - Flag axis plumbed: `androidEmbedderApi` parameter plumbed through `runAndroidEngineTests` and forwarded as `--android-embedder-api` / `--no-android-embedder-api` to `flutter drive`.
   - `composition_matrix_results.json` emitted: Machine-readable summary saved to disk for downstream ledger and dashboard tracking.
   - Result: All criteria satisfied.
2. **Falsification Attempt 2: Verify static analysis and formatting clean.**
   - `dart analyze --fatal-infos dev/bots/suite_runners/run_android_engine_tests.dart` exits 0 with 0 issues found.
   - `dart format` reports 0 changed files.
   - Result: Verified clean.
3. **Falsification Attempt 3: Verify ratchet gate compatibility.**
   - Python dependency ratchet `python3 engine/src/flutter/tools/android_embedder_deps.py --check` passes cleanly.
   - Result: Verified.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-0.11
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-0.11.code-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   Verified composition conformance harness implementation in run_android_engine_tests.dart featuring explicit mode registry, empty-mode assertions, self-documenting HCPP Vulkan requirement, OpenGLES fallback coverage, flag axis plumbing, and machine-readable JSON summary.
NEXT:      validator
```
