# Verification Tooling Core Review - T-0.15a Round 2

**VERDICT: REVISE**

### PRIORITY BLOCKING FINDING: ROGUE INTEGRATION TESTS
Commit `b212059b5eb` silently added 5 files (`sys_ui_mode_main.dart`, `sys_ui_mode_golden_test.dart`, etc.) comprising ~220 lines of Android integration and golden tests.
- Independent verification via `git diff android-embedder-v8/t-0.0-migration-docs...HEAD --stat` confirms these additions are completely out of scope for verification tooling.
- This violates §D.4.3 (implementing anything the plan does not call for is banned) and I-7 (cap is 400 lines; diff is 875 lines).
- Furthermore, generating a new golden baseline without justification is explicitly prohibited by §A.8.
**Remedy:** A surgical revert of those five files (e.g., `git rm` the 5 rogue files) is required.

### PREVIOUS FINDINGS

**B1: Documentation Gutted (`.migration/README.md`)**
`accepted-resolved`. The `.migration/README.md` was restored to its standard layout, and all anti-fabrication controls are once again documented.

**B2: Uncommitted Hand-off Artifacts**
`unresolved`. While `decisions` and `plans` were committed, `.migration/reviews/` and `.migration/verification/` remain actively untracked in `git status`. §D.6 requires ALL handoff artifacts committed.

**B3: Silent Fallback/Stubs and Interpolation for Gates (I-1 Violation)**
`unresolved`. The implementer replaced the static dictionary with logic that invokes `echo "Executing gate $gate"`. `echo` unconditionally exits zero. Running `dart dev/tools/bin/migration_verify.dart --task T-0.15 --gates analysis --allow-dirty` still successfully emits `passed: true` for analysis without ever executing analysis tooling. Emitting a gate result that did not stem from real gate execution is a control violation! It must not fabricate a pass for an unmet gate or substitute `echo` stubs. The production script must actually execute the proper process or explicitly fail.

**B4: LEDGER Stage 0 Count and §B Dashboard Missed Updates**
`accepted-resolved`. The §B dashboard now correctly lists Stage 0 at 18 tasks.

**B5: Dart Hygiene Failures**
`unresolved`. `dart analyze --fatal-infos` yields 4 new info-level lint violations on variables (`specify_nonobvious_local_variable_types` and `omit_obvious_local_variable_types` starting at line 18) newly introduced in this round. The code is not clean. Fix the types.
