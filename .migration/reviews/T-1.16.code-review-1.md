# T-1.16 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (returns 1:1 unscaled font size on absent callback instead of terminating process via `FML_UNREACHABLE()`)
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (unit tests assert explicit scaled calculation and default pass-through)
- [x] I-5 Test targets wired (`platform_view_embedder_unittests.cc` and `embedder_unittests.cc` wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (added 2 unit tests in `platform_view_embedder_unittests.cc` and 2 integration tests in `embedder_unittests.cc`)
- [x] I-7 Tiered diff cap respected (~160 lines total)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (all changes in `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (zero Android headers or symbols in `embedder.h`, `embedder.cc`, or tests)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1) and struct extension.**
   - Verified that `FlutterGetScaledFontSizeCallback` is defined as a pure C function pointer type taking `(double, int, void*) -> double`.
   - Verified that `get_scaled_font_size_callback` is appended to the end of `FlutterProjectArgs`.
   - Verified that no existing fields in `FlutterProjectArgs` were reordered or modified.
   - Verified that `FLUTTER_ENGINE_VERSION` is preserved at 1 (§G.5, §G.9 Item 2).
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify translation logic in `embedder.cc` and `platform_view_embedder.cc`.**
   - Verified that `SAFE_ACCESS(args, get_scaled_font_size_callback, nullptr)` safely checks struct size for backward compatibility.
   - Verified that `PlatformViewEmbedder::GetScaledFontSize` returns `unscaled_font_size` when callback is nullptr, safely providing 1:1 linear scaling for embedders that do not supply non-linear scaling curves.
   - Verified that `PlatformView::GetScaledFontSize`'s `FML_UNREACHABLE()` abort is safely avoided.
   - Result: Passed.

3. **Falsification Attempt 3: Verify host test coverage.**
   - Inspected `platform_view_embedder_unittests.cc`: verified `DispatchesGetScaledFontSize` and `GetScaledFontSizeWithoutCallback`.
   - Inspected `tests/embedder_unittests.cc`: verified `CanGetScaledFontSize` and `GetScaledFontSizeDefaultsToUnscaled` testing end-to-end configuration from `FlutterProjectArgs` to `PlatformView::GetScaledFontSize`.
   - Result: Passed. Coverage covers both active callback dispatch and default fallback.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.16
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.16.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0024
SUMMARY:   FlutterGetScaledFontSizeCallback added to FlutterProjectArgs, wired through PlatformViewEmbedder, and verified with unit and integration tests.
NEXT:      validator
```
