# T-1.1 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (host embedder test exercises setup_callback on raster thread)
- [x] I-5 Test targets wired (`embedder_unittests` runs `embedder_unittests.cc` and `embedder_gl_unittests.cc`)
- [x] I-6 Net coverage preserved (added 2 new test cases)
- [x] I-7 Tiered diff cap respected (well within Tier 1 ~150 lines cap)
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (only `shell/platform/embedder/` touched)
- [x] I-10 No Android in shared embedder code (zero Android headers or symbols in `embedder.h`, `embedder.cc`, `platform_view_embedder.*`)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1).**
   - Verified that `setup_callback` is strictly appended to `FlutterOpenGLRendererConfig`, `FlutterMetalRendererConfig`, and `FlutterVulkanRendererConfig`.
   - Verified that `SAFE_ACCESS(config, setup_callback, nullptr)` is used in `embedder.cc` with `nullptr` fallback.
   - Verified that `FLUTTER_ENGINE_VERSION` is untouched.
   - Verified that zero Android types or headers are present in `shell/platform/embedder/`.
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify thread affinity and dispatch lifecycle.**
   - Inspected `SetupImpellerContext()` and `CreateRenderingSurface()` in `PlatformViewEmbedder`. Both execute on the raster thread.
   - Verified the `renderer_setup_called_` idempotency guard prevents double invocation if both Impeller context setup and surface creation occur.
   - Result: Passed. Setup callback executes once on the raster task runner before GPU surface or Impeller context creation.

3. **Falsification Attempt 3: Verify host test coverage.**
   - Inspected `TEST(EmbedderTestNoFixture, RendererConfigSetupCallbackFields)` in `embedder_unittests.cc` for compile-time and struct ABI verification.
   - Inspected `TEST_F(EmbedderTest, RendererConfigSetupCallback)` in `embedder_gl_unittests.cc` which registers `setup_callback`, launches the engine, verifies raster thread invocation, and asserts `user_data` pointer equality with `&context`.
   - Result: Passed. Complete host-side non-Android test coverage.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.1
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.1.code-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   C-ABI extension, PlatformViewEmbedder dispatch, and host unit tests for FlutterRendererSetupCallback strictly adhere to migration invariants and Stage 1 C-ABI rules.
NEXT:      validator
```
