# T-1.18a Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (proc functions explicitly check for null merger handle; zero-initialization ensures dynamic merging is disabled by default)
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (proc table unit test verifies new entries are populated, unique, and callable)
- [x] I-5 Test targets wired (`embedder_unittests_proctable.cc` wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (added unit tests in `embedder_unittests_proctable.cc`)
- [x] I-7 Tiered diff cap respected (~180 lines total across headers and implementation)
- [x] I-8 No nogncheck without bug (zero new nogncheck)
- [x] I-9 No shared-code changes outside embedder layer (all changes in `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (pure C-ABI, zero Android headers or types)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules and struct extension.**
   - Verified that `FlutterRasterThreadMergerRef` is defined as an opaque pointer type.
   - Verified that `FlutterFrameThreadingInfo` includes `struct_size`.
   - Verified that `FlutterCompositor` fields are appended after `present_view_callback`.
   - Verified that `FLUTTER_ENGINE_VERSION = 1` is preserved (§G.5/G.9).
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify proc table safety and backward compatibility.**
   - Verified that `FlutterEngineGetProcAddresses` uses `STRUCT_HAS_MEMBER` to guard all writes, preventing buffer overflows if caller specifies an older `struct_size`.
   - Verified that `FlutterRasterThreadMergerIsMerged`, `FlutterRasterThreadMergerIsOnPlatformThread`, `FlutterRasterThreadMergerMergeWithLease`, and `FlutterRasterThreadMergerExtendLeaseTo` safely handle null inputs without throwing or crashing.
   - Result: Passed.

3. **Falsification Attempt 3: Verify proc table test verification.**
   - Inspected `embedder_unittests_proctable.cc`: verified that `AllPointersProvided` iterates through all function pointers up to `struct_size` and confirms they are non-null.
   - Inspected `NoDuplicatePointers` verifying no copy-paste errors or duplicate function pointers in the proc table.
   - Verified `RasterThreadMergerProcs` unit test exercising all four accessors with null handles.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.18a
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.18a.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0025
SUMMARY:   Dynamic thread merging C-ABI types and FlutterEngineProcTable entries added, implemented, and verified with proc table unit tests.
NEXT:      validator
```
