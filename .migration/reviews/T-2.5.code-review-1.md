# T-2.5 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Thread priorities, thread mask logic, and thread names match existing behavior exactly)
- [x] I-2 Assertions not weakened (All thread assertions, FML_DCHECKs, and message loop checks retained)
- [x] I-3 Tests not weakened (Pre-existing characterization tests in `android_threading_unittests.cc` pass unmodified)
- [x] I-4 Parity proven by test (Unit tests verify `FlutterCustomTaskRunners` layout, priorities, and `AndroidShellHolder` task runner exposure)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added unit tests covering `AndroidTaskRunners` directly)
- [x] I-7 Tiered diff cap respected (Clean extraction well below <= 1000 lines diff cap)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Changes strictly confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Embedder API headers used cleanly)
- [x] I-11 All composition modes preserved (Thread priorities and affinities across all composition modes preserved)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify `AndroidTaskRunners` shape and `FlutterCustomTaskRunners` parity.**
   - Verified that `AndroidTaskRunners` exposes both `flutter::TaskRunners` (for Stage 2 `Shell`) and `FlutterCustomTaskRunners` (for Stage 3 Embedder API).
   - Validated `struct_size` for `FlutterCustomTaskRunners` and each `FlutterTaskRunnerDescription`.
   - Validated priority mappings: platform=`kNormal`, raster=`kRaster`, ui=`kDisplay`, io=`kNormal`.
   - Verified `AndroidPlatformThreadPrioritySetter` matches `FlutterThreadPrioritySetter` signature and accurately maps priorities to POSIX `setpriority` (-1 UI, -5/-2 Raster, 10 IO, 0 Normal) and CPU affinities.
   - Result: Passed (verified via `AndroidTaskRunnersCustomTaskRunnersShape` in `android_threading_unittests.cc`).

2. **Falsification Attempt 2: Verify `AndroidShellHolder` integration and lifecycle equivalence.**
   - Verified that `AndroidShellHolder` constructs `AndroidTaskRunners` via `AndroidTaskRunners::Create(...)` and removes direct `ThreadHost` dependencies.
   - Verified `Spawn()` properly reuses `task_runners_` with shared thread host, maintaining thread lifetime without spawning redundant threads.
   - Verified `android_threading_unittests.cc` tests pass without regression.
   - Result: Passed.

3. **Falsification Attempt 3: Dependency ratchet and include neutrality.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 123, net reduction of 2 internal includes in `flutter_shell_native_src`: 96 vs baseline 98).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` succeeds with exit code 0.
   - Verified `bin/cache/dart-sdk/bin/dart analyze --fatal-infos dev/tools/` succeeds with 0 issues.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-2.5
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-2.5.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0034
SUMMARY:   Successfully extracted thread configuration into standalone AndroidTaskRunners matching FlutterCustomTaskRunners shape. Decoupled ThreadHost from AndroidShellHolder, verified all invariants, tests, ratchet counts (121 <= 123 includes, 63 <= 63 deps), and code formatting.
NEXT:      validator
```
