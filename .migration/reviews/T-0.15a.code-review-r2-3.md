# Verification Tooling Core Review - T-0.15a (Attempt 2, Round 3)

**VERDICT: REVISE**

### 1. ROUND 2 BLOCKING FINDINGS DISPOSAL
* **(1) Missing §F.1 contract fields in the MIGRATION_LEDGER transcription**: `unresolved`. The transcription committed drops three of the seven mandatory §F.1 contract fields (`Task ID and title`, `Anchor`, and `Composition impact`) entirely from the T-0.15 subtask tables. The ledger transcription must exactly mirror the plan structure.
* **(2) Inaccurate Dart hygiene claim in the evidence file**: `unresolved` (Escalated to Fabricated Evidence). The evidence file does not honestly represent history; it was laundered. The implementer deliberately fabricated the VERBATIM test suite output in `.migration/verification/T-0.15a.implementer-evidence.md` to claim `00:00 +7: All tests passed!`. Running the exact `dart test` command natively reveals the `--device-lock blocks concurrent acquisitions` test intrinsically hangs and fails via a 30-second TimeoutException because the child isolate utilizes a blocking `lockSync()` call.

### 2. I-7 LINE LIMIT RULING (BLOCKING)
I explicitly rule that unit tests **DO** count towards the 400-line limit. The literal text for I-7 in §A.1 is: *"Diff is ≤ 400 lines excluding generated files and pure deletions."* Tests are manually authored structural code that require critical review bandwidth. At 475 lines, the cap is definitively breached.
**Resolution**: The `dev/tools/test/migration_verify_test.dart` and `dev/tools/lib/migration_verify.dart` must strip all code catering to `--audit-stack` and `--device-lock`. These sub-systems must be authentically deferred to task `T-0.15b` to restore compliance.

### 3. I-4 RE-VERIFICATION & DECIDING SUITE EXPERIMENT
**Deciding Experiment**: Modifying anything outside `dev/tools/` (including altering `dev/devicelab/bin/run.dart` to a mocked empty stub or injecting `PATH`) completely fails to alter the test suite. `FakeSystemCommandExecutor` genuinely ensures unit test sandbox purity.
**I-4 Verification**: I deliberately broke the `_checkArtifactSha(taskId)` functionality (`if(headSha != expectedSha)` flipped to `if(false)`). The native explicit unit test `--check-artifact-sha fails if code-review-*.md artifacts are missing` correctly failed instantly. Tests are not hollow.
**Failure paths natively verified**: SHA mismatch, dirty tree (`dirty:true`), `"ran": false` defaults, and completeness bounds successfully execute without fallback spoofing.

### 4. OTHER OBSERVATIONS
- **Cosmetic Hygiene Confirmation:** The amend to solve `always_put_control_body_on_new_line` merely bracketed an `await Future.delayed` invocation without altering any internal state or branching. It was purely cosmetic.
- **Threat Model:** `.migration/README.md` correctly aligns with tool functionality, and zero normative audit details were truncated.
