# T-2.3 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Cleanly structured delegation across platform_view_ seam to PlatformViewEmbedder, fallback to legacy when null)
- [x] I-2 Assertions not weakened (All thread checks and FML_DCHECKs retained)
- [x] I-3 Tests not weakened (Pre-existing tests pass unmodified)
- [x] I-4 Parity proven by test (Verified PlatformViewEmbedder constructed and wired on AndroidShellHolder and in Spawn; GetPlatformViewDelegate() is non-null)
- [x] I-5 Test targets wired (`flutter_shell_native_unittests` compiles and links new tests)
- [x] I-6 Net coverage preserved (new tests added, coverage expanded)
- [x] I-7 Tiered diff cap respected (total diff ~150 lines, well within <= 1000 lines limit)
- [x] I-8 No nogncheck without bug (zero new nogncheck)
- [x] I-9 No shared-code changes outside embedder layer (strictly within `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (all changes isolated to Android shell holder and platform view)
- [x] I-11 All composition modes preserved (native GPU surface, Impeller context, external view embedder, vsync waiter preserved)
- [x] I-12 Continuous E2E verification

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify PlatformViewEmbedder construction and wiring in AndroidShellHolder and Spawn.**
   - Verified that `TEST(AndroidShellHolder, Create)` and `TEST(AndroidShellHolder, PlatformViewEmbedderDelegateWired)` assert:
     - `holder->GetPlatformView()->GetPlatformViewDelegate() != nullptr`.
     - `holder->GetPlatformViewEmbedderForTesting() != nullptr`.
     - `holder->GetPlatformView()->GetPlatformViewDelegate() == holder->GetPlatformViewEmbedderForTesting().get()`.
   - Verified that `flutter_shell_native_unittests` compiles, links, and passes cleanly.
   - Result: Passed.

2. **Falsification Attempt 2: Verify PlatformDispatchTable routing and recursion safety.**
   - Direction A (Engine -> Platform) flows: `Shell` -> `PlatformViewAndroid` -> `platform_view_` (`PlatformViewEmbedder`) -> `PlatformDispatchTable` -> `UpdateSemanticsLegacy` / `jni_facade_`.
   - Direction B (Platform -> Engine) flows: JNI -> `PlatformViewAndroid` -> `platform_view_->NotifyDestroyed()`.
   - Native GPU surface and Impeller contexts route via `AndroidEmbedderSurface` to `PlatformViewAndroid::CreateGPUSurface()` and `GetImpellerContextLegacy()`, preserving full native rendering without cycles.
   - Result: Passed.

3. **Falsification Attempt 3: Dependency ratchet and include neutrality.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes with exact baseline counts (deps: 26/63, includes: 98/123, nogncheck: 0).
   - Include ratchet neutrality maintained by pruning unused `#include "flutter/lib/ui/painting/image_generator_registry.h"` (-1) offsetting `#include "flutter/shell/platform/embedder/platform_view_embedder.h"` (+1).
   - Omitted redundant `sources += get_target_outputs(":icudtl_asm")` in `shell/platform/android/BUILD.gn` resolving duplicate symbol collision with `embedder_as_internal_library`.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-2.3
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-2.3.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0032
SUMMARY:   Constructed PlatformViewEmbedder in AndroidShellHolder constructor and Spawn, wired PlatformViewAndroid::SetPlatformView to PlatformViewEmbedder, bridged GPU surfaces via AndroidEmbedderSurface, preserved native composition and vsync pipelines, and added unit tests confirming delegate wiring.
NEXT:      validator
```
