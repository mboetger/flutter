# T-1.9 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (Vulkan external texture resolution verified with host tests)
- [x] I-5 Test targets wired (`embedder_vk_unittests.cc` wired in `BUILD.gn`)
- [x] I-6 Net coverage preserved (new Vulkan external texture and backing store tests added)
- [x] I-7 Tiered diff cap respected
- [x] I-8 No nogncheck without bug (zero nogncheck introduced)
- [x] I-9 No shared-code changes outside embedder layer (all changes in `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (zero Android headers, types, or symbols in `embedder.h`, `embedder.cc`, or tests)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

## Falsification Attempts
1. **Falsification Attempt 1: Verify C-ABI rules (§6.1) and struct extension.**
   - Verified that `vulkan_external_texture_frame_callback` is appended to the end of `FlutterVulkanRendererConfig`.
   - Verified that `FlutterVulkanExternalTexture` is defined with explicit `struct_size` field and standard layout.
   - Verified that `FLUTTER_ENGINE_VERSION` is untouched (§G.5, §G.9 Item 2).
   - Verified that `SAFE_ACCESS(vulkan_config, vulkan_external_texture_frame_callback, nullptr)` is used in `embedder.cc`.
   - Result: Passed. Strict C-ABI conformance maintained.

2. **Falsification Attempt 2: Verify Vulkan external texture lifecycle and resolution.**
   - Verified that `EmbedderExternalTextureVK` correctly handles both Skia (Ganesh) and Impeller pipelines.
   - Verified that `destruction_callback` is reliably invoked when textures are collected, ensuring no GPU memory leaks or orphaned fences.
   - Verified that `EmbedderExternalTextureResolver` properly dispatches to Vulkan when `vulkan_callback_` is set.
   - Result: Passed. Complete lifecycle management maintained.

3. **Falsification Attempt 3: Verify host test coverage.**
   - Inspected `tests/embedder_unittests.cc`:
     - `RendererConfigVulkanExternalTextureFields` validates struct fields, initialization, and callback signatures.
   - Inspected `tests/embedder_vk_unittests.cc`:
     - `VulkanExternalTextureRegistrationAndLifecycle` tests registration, frame availability signaling, and unregistration.
     - `VulkanExternalTextureWithLegacyStructSize` tests backward compatibility when `struct_size` is truncated before the new callback.
     - `VulkanExternalTextureCallbackFailureHandling` tests graceful handling when callback returns false.
   - Result: Passed. Host test coverage is comprehensive.

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.9
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.9.code-review-1.md
BLOCKING:  0
DECISIONS: none
SUMMARY:   Vulkan external texture callbacks and structs added to embedder.h, resolved in EmbedderExternalTextureVK, wired into EmbedderExternalTextureResolver, supported in Impeller Vulkan backing stores, and verified by host unit tests.
NEXT:      validator
```
