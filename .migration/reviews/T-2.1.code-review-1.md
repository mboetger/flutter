# T-2.1 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Seam has explicit delegation semantics: forwards when delegate is set, falls back to base class PlatformView::NotifyDestroyed when null)
- [x] I-2 Assertions not weakened (Retains all thread assertions and latches)
- [x] I-3 Tests not weakened (Pre-existing characterization tests pass unmodified)
- [x] I-4 Parity proven by test (`PlatformViewDelegateSeam` and `NotifyDestroyedDefaultFallback` in `platform_view_android_unittests.cc` exercise delegation and fallback)
- [x] I-5 Test targets wired (`flutter_shell_native_unittests` compiles and executes new tests)
- [x] I-6 Net coverage preserved (new tests added, coverage expanded)
- [x] I-7 Tiered diff cap respected (total diff ~170 lines, well within <= 1000 lines limit)
- [x] I-8 No nogncheck without bug (zero new nogncheck)
- [x] I-9 No shared-code changes outside embedder layer (strictly within `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (all changes isolated to Android platform view)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

---

## Falsification Attempts
1. **Falsification Attempt 1: Delegate registration, accessor, and invocation forwarding.**
   - Verified that `PlatformViewAndroidTest.PlatformViewDelegateSeam` registers a `MockPlatformViewDelegate` via `SetPlatformView()`, checks that `GetPlatformViewDelegate()` matches, calls `NotifyDestroyed()`, and verifies `delegate->notify_destroyed_called == true`.
   - Verified that clearing the delegate with `SetPlatformView(nullptr)` restores `GetPlatformViewDelegate() == nullptr`.
   - Result: Passed.

2. **Falsification Attempt 2: Fallback when delegate is null.**
   - Verified that `PlatformViewAndroidTest.NotifyDestroyedDefaultFallback` invokes `NotifyDestroyed()` with `platform_view_ == nullptr`.
   - Confirmed that execution falls through safely to base `PlatformView::NotifyDestroyed()` and performs surface destruction without crashing, asserting, or leaking resources.
   - Result: Passed.

3. **Falsification Attempt 3: Dependency ratchet and C++ header isolation.**
   - Verified that `platform_view_android_unittests.cc` avoids `#include "flutter/shell/common/shell.h"` by using a local `FakePlatformViewDelegate : public PlatformView::Delegate`.
   - Verified that running `python3 engine/src/flutter/tools/android_embedder_deps.py --check` passes with exact baseline counts (63 deps, 123 includes, 0 nogncheck).
   - Verified that renaming `struct AndroidSurface::Screenshot` to `struct AndroidSurface::ScreenshotResult` resolves all C++ name-hiding compiler errors across all Android surface implementations (`dynamic_impeller`, `gl_impeller`, `gl_skia`, `software`, `mock`).
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-2.1
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-2.1.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0028
SUMMARY:   PlatformView delegate pointer seam on PlatformViewAndroid implemented with accessor methods and NotifyDestroyed forwarding, unit tests added in platform_view_android_unittests.cc, and C++ struct name collision resolved across AndroidSurface implementations.
NEXT:      validator
```
