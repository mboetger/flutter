# T-1.18b Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (`InferExternalViewEmbedderFromArgs` explicitly fails with `kInvalidArgument` if `supports_dynamic_thread_merging = true` without `post_preroll_callback`)
- [x] I-2 Assertions not weakened
- [x] I-3 Tests not weakened
- [x] I-4 Parity proven by test (proc table tests verify functions callable; inertness verified)
- [x] I-5 Test targets wired
- [x] I-6 Net coverage preserved
- [x] I-7 Tiered diff cap respected (136 insertions, 9 deletions across 3 files; well within <= 1000 lines)
- [x] I-8 No nogncheck without bug (zero new nogncheck)
- [x] I-9 No shared-code changes outside embedder layer (all changes in `shell/platform/embedder/`)
- [x] I-10 No Android in shared embedder code (pure generic embedder code, zero Android-specific headers)
- [x] I-11 All composition modes preserved
- [x] I-12 Continuous E2E verification

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify Invariant I-1 enforcement in InferExternalViewEmbedderFromArgs.**
   - Verified that `supports_dynamic_thread_merging` and `post_preroll_callback` are read using `SAFE_ACCESS`.
   - Verified that if `supports_dynamic_thread_merging` is true and `c_post_preroll_callback` is null, the function returns `fml::Status(fml::StatusCode::kInvalidArgument, ...)`.
   - Verified that callers log the error and halt initialization with `kInvalidArguments`.
   - Result: Passed. Strict enforcement of I-1.

2. **Falsification Attempt 2: Verify lifetime and memory safety of threading info.**
   - Verified that `_FlutterRasterThreadMerger` and `FlutterFrameThreadingInfo` are constructed on the stack in `PostPrerollAction`, `BeginFrame`, and `EndFrame`.
   - Verified that `raster_thread_merger ? &merger_wrapper : nullptr` guards against null mergers when thread merging is disabled or statically merged.
   - Verified that `struct _FlutterRasterThreadMerger` is defined in `embedder_external_view_embedder.h` in the global namespace, matching `embedder.h` typedef.
   - Result: Passed. Zero heap allocations and deterministic stack lifetime conforming to the API contract.

3. **Falsification Attempt 3: Verify mapping between FlutterPostPrerollResult and PostPrerollResult.**
   - Verified `ToPostPrerollResult` maps:
     - `kFlutterPostPrerollResultSuccess` -> `PostPrerollResult::kSuccess`
     - `kFlutterPostPrerollResultResubmitFrame` -> `PostPrerollResult::kResubmitFrame`
     - `kFlutterPostPrerollResultSkipAndRetryFrame` -> `PostPrerollResult::kSkipAndRetryFrame`
   - Verified that unexpected values are logged with `FML_LOG(ERROR)` and default safely to `PostPrerollResult::kSuccess`.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-1.18b
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-1.18b.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0026
SUMMARY:   Dynamic thread merging overrides implemented in EmbedderExternalViewEmbedder and wired into InferExternalViewEmbedderFromArgs in embedder.cc.
NEXT:      validator
```
