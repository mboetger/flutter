# T-3.6 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Flag state is explicitly branched; UnregisterExternalTexture and MarkExternalTextureFrameAvailable return explicit FlutterEngineResult)
- [x] I-2 Assertions not weakened (Argument validation and texture identifier checks strictly preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify texture unregister and mark frame available flag-off, flag-on, and direct seam dispatch)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added 7 new unit tests in `platform_view_android_unittests.cc` covering flag-off, flag-on, direct seam invocation, and invalid argument edge cases)
- [x] I-7 Tiered diff cap respected (Production diff ~60 lines, total diff ~440 lines including tests and artifacts, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses platform-agnostic `FlutterEngineResult`)
- [x] I-11 All composition modes preserved (External texture unregister and frame availability work across all texture composition modes)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify legacy vs embedder path separation and tracing for UnregisterTexture.**
   - Verified default/flag off emits `TRACE_EVENT1("flutter", "PlatformViewAndroid::UnregisterTexture", "path", "legacy")` and routes through `external_texture_adapter_` / `platform_view_`.
   - Verified flag on emits `TRACE_EVENT1("flutter", "PlatformViewAndroid::UnregisterTexture", "path", "embedder_api")` and routes through `UnregisterExternalTexture(texture_id)`.
   - Result: Passed (verified via `PlatformViewAndroidTest.UnregisterTextureDispatchLegacyPath` and `UnregisterTextureDispatchEmbedderApiPath`).

2. **Falsification Attempt 2: Verify legacy vs embedder path separation and tracing for MarkTextureFrameAvailable.**
   - Verified default/flag off emits `TRACE_EVENT1("flutter", "PlatformViewAndroid::MarkTextureFrameAvailable", "path", "legacy")` and routes through `external_texture_adapter_` / `platform_view_`.
   - Verified flag on emits `TRACE_EVENT1("flutter", "PlatformViewAndroid::MarkTextureFrameAvailable", "path", "embedder_api")` and routes through `MarkExternalTextureFrameAvailable(texture_id)`.
   - Result: Passed (verified via `PlatformViewAndroidTest.MarkTextureFrameAvailableDispatchLegacyPath` and `MarkTextureFrameAvailableDispatchEmbedderApiPath`).

3. **Falsification Attempt 3: Verify direct seam invocation and input validation.**
   - Verified calling `UnregisterExternalTexture` and `MarkExternalTextureFrameAvailable` with valid texture ID returns `kSuccess` and delegates properly.
   - Verified calling with non-positive texture ID (<= 0) returns `kInvalidArguments`.
   - Result: Passed (verified via `PlatformViewAndroidTest.UnregisterExternalTextureDirect`, `MarkExternalTextureFrameAvailableDirect`, and `TextureSeamInvalidArguments`).

4. **Falsification Attempt 4: Dependency ratchet and build cleanliness.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-3.6
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-3.6.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0046
SUMMARY:   Successfully implemented UnregisterExternalTexture and MarkExternalTextureFrameAvailable C-ABI seams in PlatformViewAndroid and routed UnregisterTexture and MarkTextureFrameAvailable behind --android-embedder-api with TRACE_EVENT logging and zero dual-routing. Added unit tests for legacy paths, embedder paths, direct seam invocation, and invalid argument handling. Ratchet counts (63 deps, 121 includes, 0 nogncheck) and build pass cleanly.
NEXT:      validator
```
