# T-3.8 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Flag state is explicitly branched; SpawnEngine returns explicit FlutterEngineResult)
- [x] I-2 Assertions not weakened (Shell setup preconditions and platform view assertions strictly preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify engine spawning flag-off, flag-on, direct seam dispatch, and validation edge cases)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added 4 new unit tests in `android_shell_holder_unittests.cc` covering legacy path, embedder path, direct seam invocation, and argument validation)
- [x] I-7 Tiered diff cap respected (Production diff ~120 lines, total diff ~439 lines including tests and artifacts, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses standard `FlutterEngineSpawnConfig` and `FlutterEngineResult`)
- [x] I-11 All composition modes preserved (Engine spawning works across all composition modes)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify legacy vs embedder path separation and tracing for Spawn.**
   - Verified default/flag off emits `TRACE_EVENT1("flutter", "AndroidShellHolder::Spawn", "path", "legacy")` and runs legacy creation.
   - Verified flag on emits `TRACE_EVENT1("flutter", "AndroidShellHolder::Spawn", "path", "embedder_api")` and routes through `SpawnEngine(...)`.
   - Result: Passed (verified via `AndroidShellHolder.SpawnLegacyPath` and `AndroidShellHolder.SpawnEmbedderApiPath`).

2. **Falsification Attempt 2: Verify direct seam invocation.**
   - Verified calling `SpawnEngine(...)` with custom entrypoint and initial route delegates through run configuration.
   - Result: Passed (verified via `AndroidShellHolder.SpawnEngineDirect`).

3. **Falsification Attempt 3: Verify argument validation in SpawnEngine.**
   - Verified null spawned_holder_out returns `kInvalidArguments`.
   - Verified null config returns `kInvalidArguments`.
   - Verified invalid struct_size returns `kInvalidArguments`.
   - Verified negative argc returns `kInvalidArguments`.
   - Verified null argv with argc > 0 returns `kInvalidArguments`.
   - Verified null element in argv returns `kInvalidArguments`.
   - Result: Passed (verified via `AndroidShellHolder.SpawnEngineInvalidArguments`).

4. **Falsification Attempt 4: Dependency ratchet and build cleanliness.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-3.8
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-3.8.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0048
SUMMARY:   Successfully implemented SpawnEngine C-ABI seam in AndroidShellHolder and routed Spawn behind --android-embedder-api with TRACE_EVENT logging and zero dual-routing. Added unit tests for legacy path, embedder path, direct seam invocation, and thorough argument validation. Ratchet counts (63 deps, 121 includes, 0 nogncheck) and build pass cleanly.
NEXT:      validator
```
