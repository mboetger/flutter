# T-3.T6 Code Review 1

## Invariant Audit (§A.1)
- [x] I-1 No silent fallbacks (Flag state is explicitly branched; seams return explicit FlutterEngineResult)
- [x] I-2 Assertions not weakened (All invariant checks strictly preserved)
- [x] I-3 Tests not weakened (All pre-existing characterization and unit tests preserved unmodified)
- [x] I-4 Parity proven by test (Unit tests verify HC frame initiation, completion, overlay creation, overlay destruction flag-off, flag-on, direct seam dispatch, and validation edge cases)
- [x] I-5 Test targets wired (`flutter_shell_native` and `flutter_shell_native_unittests` compile and link cleanly)
- [x] I-6 Net coverage preserved (Added 8 new unit tests in `platform_view_android_unittests.cc` covering legacy path, embedder path, direct seam invocation, and argument validation)
- [x] I-7 Tiered diff cap respected (Production diff ~130 lines, total diff ~280 lines excluding artifacts, well below 1000 lines)
- [x] I-8 No nogncheck without bug (Zero new nogncheck comments)
- [x] I-9 No shared-code changes outside embedder layer (Confined to `shell/platform/android`)
- [x] I-10 No Android in shared embedder code (Uses standard `FlutterEngineResult`)
- [x] I-11 All composition modes preserved (HC mode migrated independently; all six composition modes in §B.1 are now embedder-behind-flag)
- [x] I-12 Continuous E2E verification (Dart analysis, dependency ratchet, and native build pass)

---

## Falsification Attempts
1. **Falsification Attempt 1: Verify legacy vs embedder path separation and tracing for BeginFrameHC.**
   - Verified default/flag off emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::BeginFrameHC", "mode", "HC", "path", "legacy")` and calls `jni_facade_->FlutterViewBeginFrame()`.
   - Verified flag on emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::BeginFrameHC", "mode", "HC", "path", "embedder_api")` and routes through `BeginFrameHCEmbedder()`.
   - Result: Passed (verified via `PlatformViewAndroidTest.BeginFrameHCLegacyPath`, `PlatformViewAndroidTest.BeginFrameHCEmbedderApiPath`, and `PlatformViewAndroidTest.BeginFrameHCDirectSeam`).

2. **Falsification Attempt 2: Verify legacy vs embedder path separation and tracing for EndFrameHC.**
   - Verified default/flag off emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::EndFrameHC", "mode", "HC", "path", "legacy")` and calls `jni_facade_->FlutterViewEndFrame()`.
   - Verified flag on emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::EndFrameHC", "mode", "HC", "path", "embedder_api")` and routes through `EndFrameHCEmbedder()`.
   - Result: Passed (verified via `PlatformViewAndroidTest.EndFrameHCLegacyPath`, `PlatformViewAndroidTest.EndFrameHCEmbedderApiPath`, and `PlatformViewAndroidTest.EndFrameHCDirectSeam`).

3. **Falsification Attempt 3: Verify legacy vs embedder path separation, tracing, and validation for CreateOverlaySurfaceHC.**
   - Verified default/flag off emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::CreateOverlaySurfaceHC", "mode", "HC", "path", "legacy")` and calls `jni_facade_->FlutterViewCreateOverlaySurface()`.
   - Verified flag on emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::CreateOverlaySurfaceHC", "mode", "HC", "path", "embedder_api")` and routes through `CreateOverlaySurfaceHCEmbedder(...)`.
   - Verified passing nullptr to `CreateOverlaySurfaceHCEmbedder` returns `kInvalidArguments`, while valid pointer returns `kSuccess` with non-null metadata.
   - Result: Passed (verified via `PlatformViewAndroidTest.CreateOverlaySurfaceHC`).

4. **Falsification Attempt 4: Verify legacy vs embedder path separation and tracing for DestroyOverlaySurfacesHC.**
   - Verified default/flag off emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::DestroyOverlaySurfacesHC", "mode", "HC", "path", "legacy")` and calls `jni_facade_->FlutterViewDestroyOverlaySurfaces()`.
   - Verified flag on emits `TRACE_EVENT2("flutter", "PlatformViewAndroid::DestroyOverlaySurfacesHC", "mode", "HC", "path", "embedder_api")` and routes through `DestroyOverlaySurfacesHCEmbedder()`.
   - Result: Passed (verified via `PlatformViewAndroidTest.DestroyOverlaySurfacesHC`).

5. **Falsification Attempt 5: Composition mode convergence and B.1 tracker update.**
   - Verified §B.1 composition mode convergence tracker in `MIGRATION_LEDGER.md` is updated on this branch to `embedder-behind-flag` verified by `T-3.T6`.
   - Verified that all six composition modes in §B.1 (VD, TLHC, HC, HCPP, SurfaceTexture, SurfaceProducer/ImageReader) are now `embedder-behind-flag`.
   - Result: Passed.

6. **Falsification Attempt 6: Dependency ratchet and build cleanliness.**
   - Verified `python3 tools/android_embedder_deps.py --check` passes:
     - Deps: 63 (baseline 63).
     - Includes: 121 (baseline 121).
     - nogncheck: 0.
   - Verified `ninja -C out/android_debug_unopt_arm64 flutter_shell_native_unittests flutter_shell_native` compiles and links cleanly with exit code 0.
   - Result: Passed.

---

## Findings
- Blocking: 0
- Non-blocking: 0

## Verdict
APPROVE

```
ROLE:      code-reviewer
TASK:      T-3.T6
ROUND:     1
VERDICT:   APPROVE
ARTIFACT:  .migration/reviews/T-3.T6.code-review-1.md
BLOCKING:  0
DECISIONS: DR-0054
SUMMARY:   Successfully migrated HC composition mode behind --android-embedder-api with TRACE_EVENT logging, updated B.1 tracker completing all six modes, and added comprehensive unit tests. Ratchet counts (63 deps, 121 includes, 0 nogncheck) and build pass cleanly.
NEXT:      validator
```
