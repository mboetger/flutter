import java.io.FileInputStream
import java.util.Properties

group = "{{androidIdentifier}}"
version = "1.0"

buildscript {
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath("com.android.tools.build:gradle:{{agpVersion}}")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

plugins {
    id("com.android.library")
}

android {
    namespace = "{{androidIdentifier}}"

    compileSdk = {{compileSdkVersion}}

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    defaultConfig {
        minSdk = {{minSdkVersion}}
    }

    testOptions {
        unitTests.all {
            it.outputs.upToDateWhen { false }

            it.testLogging {
                events("passed", "skipped", "failed", "standardOut", "standardError")
                showStandardStreams = true
            }
        }
    }
}

dependencies {
    testImplementation("junit:junit:4.13.2")
    testImplementation("org.mockito:mockito-core:5.0.0")
}

val flutterSdkPath = run {
    val properties = Properties()
    val localPropertiesFile = project.rootProject.file("local.properties")
    if (localPropertiesFile.exists()) {
        FileInputStream(localPropertiesFile).use { properties.load(it) }
    }
    var sdkPath = properties.getProperty("flutter.sdk")
    if (sdkPath == null) {
        val exampleLocalPropertiesFile = project.rootProject.file("../example/android/local.properties")
        if (exampleLocalPropertiesFile.exists()) {
            FileInputStream(exampleLocalPropertiesFile).use { properties.load(it) }
            sdkPath = properties.getProperty("flutter.sdk")
        }
    }
    sdkPath ?: System.getenv("FLUTTER_ROOT")
}

if (flutterSdkPath != null) {
    extra.set("flutterSdkPath", flutterSdkPath)
    apply(from = "$flutterSdkPath/packages/flutter_tools/gradle/flutter_plugin_integration.gradle.kts")
}





