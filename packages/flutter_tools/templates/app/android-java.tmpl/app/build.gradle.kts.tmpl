plugins {
    id("com.android.application")
}

android {
    namespace = "{{androidIdentifier}}"
    compileSdk = 34

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    defaultConfig {
        applicationId = "{{androidIdentifier}}"
        minSdk = 21
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.getByName("debug")
        }
    }
}

dependencies {
    // Bazel will generate the flutter.aar payload in the build directory.
    implementation(files("../../build/host/outputs/repo/flutter.aar"))
}
